---
phase: 05-polish-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - components/ProjectsSection.tsx
autonomous: true
requirements:
  - ANLYT-01
must_haves:
  truths:
    - "Vercel Analytics script is injected on every page load without a cookie consent banner (D-01)"
    - "Vercel Speed Insights script is injected on every page load without cookies (D-02)"
    - "Clicking 'GitHub' on the Brew Index project card does not lead to a GitHub 404 page (D-09)"
    - "The github link is conditionally rendered — missing github field suppresses the link element"
    - "app/layout.tsx remains a Server Component (no 'use client' directive)"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with Analytics and SpeedInsights components"
      contains: "Analytics"
    - path: "components/ProjectsSection.tsx"
      provides: "Projects list with dead link removed"
  key_links:
    - from: "app/layout.tsx"
      to: "@vercel/analytics/next"
      via: "import { Analytics } from '@vercel/analytics/next'"
      pattern: "@vercel/analytics/next"
    - from: "app/layout.tsx"
      to: "@vercel/speed-insights/next"
      via: "import { SpeedInsights } from '@vercel/speed-insights/next'"
      pattern: "@vercel/speed-insights/next"
    - from: "components/ProjectsSection.tsx"
      to: "project.github (optional)"
      via: "conditional render guard"
      pattern: "project\\.github &&"
---

## Phase Goal

**As a** site owner, **I want to** capture real pageview data and ship zero dead links, **so that** the site is measurable from day one and every link a visitor clicks resolves correctly.

<objective>
Wire Vercel Analytics and Speed Insights into the root layout (per D-01, D-02) and remove the confirmed-dead brew-index GitHub link from ProjectsSection (per D-08, D-09). Both packages are already installed in node_modules — this plan is two imports, two JSX components, and one conditional render guard.

Purpose: Deliver ANLYT-01 (cookieless analytics) and close the only confirmed dead link found during pre-flight research.
Output: Updated app/layout.tsx (Analytics + SpeedInsights injected), updated components/ProjectsSection.tsx (brew-index github field removed, link conditionally rendered).
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-analytics/05-CONTEXT.md
@.planning/phases/05-polish-analytics/05-RESEARCH.md
@.planning/phases/05-polish-analytics/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Analytics and SpeedInsights to root layout (D-01, D-02)</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx — read the full file before editing; understand the existing import order, the body structure, the LazyMotion boundary, and the Footer placement
    - .planning/phases/05-polish-analytics/05-PATTERNS.md — §Pattern 1 contains the exact before/after diff; §Server Component Import Rule and §LazyMotion Boundary Rule are critical constraints
  </read_first>
  <action>
    Add two import statements to app/layout.tsx after the existing Footer import (before './globals.css'):
      import { Analytics } from '@vercel/analytics/next'
      import { SpeedInsights } from '@vercel/speed-insights/next'

    Add both JSX components inside the body element, after the Footer component, before the closing body tag. They must be placed outside the LazyMotion boundary — do NOT nest them inside LazyMotion or inside the main element.

    The resulting body order must be:
      skip-to-content link → Navbar → LazyMotion(main({children})) → Footer → Analytics → SpeedInsights

    Do NOT add 'use client' to layout.tsx. Both components are Server-Component-compatible and render as script defer tags. Do NOT change any other part of the file.
  </action>
  <verify>
    <automated>grep -n "Analytics\|SpeedInsights" /Users/yash/Personal/Portfolio/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains the line: import { Analytics } from '@vercel/analytics/next'
    - app/layout.tsx contains the line: import { SpeedInsights } from '@vercel/speed-insights/next'
    - app/layout.tsx contains the JSX tag: Analytics (self-closing or fragment form)
    - app/layout.tsx contains the JSX tag: SpeedInsights (self-closing or fragment form)
    - app/layout.tsx does NOT contain 'use client' at the top (remains a Server Component)
    - Neither Analytics nor SpeedInsights appears between the LazyMotion opening tag and its closing tag
    - Both components appear after the Footer component in the JSX
    - Running `cd /Users/yash/Personal/Portfolio && npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>
    app/layout.tsx injects both Vercel tracking scripts on every page. TypeScript passes. No 'use client' added.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix brew-index dead link in ProjectsSection (D-08, D-09)</name>
  <files>components/ProjectsSection.tsx</files>
  <read_first>
    - components/ProjectsSection.tsx — read the full file; locate the projects array (lines 3–18), the github field on the Brew Index entry, and the anchor tag rendering pattern (lines 43–51)
    - .planning/phases/05-polish-analytics/05-PATTERNS.md — §Fix option A explains the exact change: remove the github field from the Brew Index entry, add an optional interface type, and wrap the anchor tag in a conditional
    - .planning/phases/05-polish-analytics/05-RESEARCH.md — §Pre-Flight Findings Summary confirms brew-index returns HTTP 404; D-09 explicitly allows removing the link
  </read_first>
  <action>
    Apply two coordinated changes to components/ProjectsSection.tsx:

    1. Add a typed Project interface above the projects array with github as an optional field:
       interface Project { name: string; description: string; tags: string[]; github?: string }
       Annotate the projects const: const projects: Project[] = [...]

    2. Remove the github field entirely from the Brew Index entry in the projects array. The Nekomori entry keeps its github field unchanged.

    3. In the JSX map, wrap the entire anchor tag element in a conditional: {project.github && (...anchor tag...)}. The anchor tag itself is unchanged — same href, target, rel, aria-label, className, and inner text.

    The Nekomori card continues to show "GitHub →". The Brew Index card shows no GitHub link at all (no broken link, no placeholder text).
  </action>
  <verify>
    <automated>grep -n "brew-index\|project\.github" /Users/yash/Personal/Portfolio/components/ProjectsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/ProjectsSection.tsx does NOT contain the string 'brew-index'
    - components/ProjectsSection.tsx contains the string 'project.github &&' (the conditional render guard)
    - components/ProjectsSection.tsx contains 'github?: string' (optional field in the Project interface)
    - The Nekomori github URL 'https://github.com/yash-278/nekomori' remains present
    - Running `cd /Users/yash/Personal/Portfolio && npx tsc --noEmit` exits 0 (no TypeScript errors)
    - Running `cd /Users/yash/Personal/Portfolio && npx next build` exits 0 (no build errors)
  </acceptance_criteria>
  <done>
    The Brew Index project card renders without a GitHub link. The dead 404 URL is gone. TypeScript and build pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout.tsx → Vercel CDN | Analytics/SpeedInsights components inject script tags that phone home to Vercel's /_vercel/insights endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Analytics script injection in layout.tsx | accept | @vercel/analytics/next renders a static script src tag; no inline code execution; no user input involved; Vercel is the hosting provider and trusted party |
| T-05-02 | Information Disclosure | Vercel Analytics data collection | accept | Cookieless by design; collects only aggregated, non-identifiable pageview data (hash-based visitor ID); confirmed against Vercel privacy policy; no consent banner required under GDPR |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Both packages already installed and slopcheck-verified [OK] before this phase; no new npm install step in this plan |
</threat_model>

<verification>
After both tasks complete:
1. Run `cd /Users/yash/Personal/Portfolio && npx tsc --noEmit` — must exit 0
2. Run `cd /Users/yash/Personal/Portfolio && npx next build` — must exit 0, must show / as static (SSG)
3. Run `grep -c "use client" /Users/yash/Personal/Portfolio/app/layout.tsx` — must output 0 (Server Component preserved)
4. Run `curl -sI https://github.com/yash-278/nekomori | head -1` — must show HTTP/2 200 (live link still present)
5. Confirm brew-index string absent: `grep -c "brew-index" /Users/yash/Personal/Portfolio/components/ProjectsSection.tsx` — must output 0
</verification>

<success_criteria>
- app/layout.tsx injects Analytics and SpeedInsights on every page without cookie banners (ANLYT-01 delivered)
- No 'use client' directive added to app/layout.tsx
- Brew Index GitHub link is gone; no 404 destination reachable from the Projects section
- TypeScript and next build both pass with exit code 0
</success_criteria>

<output>
Create `.planning/phases/05-polish-analytics/05-01-SUMMARY.md` when done.
</output>
