---
phase: 05-polish-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - components/HeroSection.tsx
  - components/AboutSection.tsx
  - components/WorkSection.tsx
  - components/ProjectsSection.tsx
  - components/ContactSection.tsx
  - components/Navbar.tsx
  - components/Footer.tsx
autonomous: false
requirements:
  - ANLYT-01
must_haves:
  truths:
    - "Animation audit confirms: no layout prop in any section component (D-06)"
    - "Animation audit confirms: every SectionReveal uses once:true — no section triggers more than once (D-06)"
    - "Animation audit confirms: only m.* imports used, no direct motion.* imports (D-06)"
    - "Lighthouse run against https://yashkadam.com completes and LCP, CLS, INP values are recorded (D-03)"
    - "All three Core Web Vitals pass: LCP < 2.5s, CLS < 0.1, INP < 200ms (D-04)"
    - "Vercel Analytics dashboard activation confirmed by human (assumption A2 from RESEARCH.md)"
  artifacts:
    - path: "components/SectionReveal.tsx"
      provides: "Spec-compliant animation primitive"
      contains: "once: true"
  key_links:
    - from: "SectionReveal (scroll mode)"
      to: "viewport once:true"
      via: "viewport: { once: true, margin: '-80px' }"
      pattern: "once.*true"
---

<objective>
Audit all scroll animations for spec compliance (D-06/D-07), run Lighthouse against the deployed production URL to get a baseline (D-03), fix any Core Web Vitals failures found (D-04/D-05), and have the human confirm Vercel dashboard activation. Phase 5 is gated on all three CWV thresholds passing.

Purpose: Certify the live site meets the Core Web Vitals bar (LCP <2.5s, CLS <0.1, INP <200ms) required by the phase success criteria. The animation audit is a read-only verification pass — pre-flight research expects it to pass clean.
Output: Animation audit results documented in the plan summary, Lighthouse report with CWV values, any required CWV fixes applied, Vercel dashboard activation confirmed.
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-analytics/05-CONTEXT.md
@.planning/phases/05-polish-analytics/05-RESEARCH.md
@.planning/phases/05-polish-analytics/05-PATTERNS.md
@.planning/phases/05-polish-analytics/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Animation audit — verify spec compliance across all section components (D-06, D-07)</name>
  <files>
    components/SectionReveal.tsx
    components/HeroSection.tsx
    components/AboutSection.tsx
    components/WorkSection.tsx
    components/ProjectsSection.tsx
    components/ContactSection.tsx
    components/Navbar.tsx
    components/Footer.tsx
  </files>
  <read_first>
    - components/SectionReveal.tsx — the spec-compliant primitive; verify variants use opacity+y only, viewport once:true, margin '-80px'; confirm no layout prop
    - components/HeroSection.tsx — uses animate='mount' mode with 6 staggered SectionReveal wrappers (delays 0–0.3s); verify it uses SectionReveal (not raw m.div with custom variants)
    - components/AboutSection.tsx — verify SectionReveal usage, no layout prop, no motion.* imports
    - components/WorkSection.tsx — verify SectionReveal usage, no layout prop, no motion.* imports
    - components/ProjectsSection.tsx — already modified in Plan 01; verify SectionReveal usage is unchanged
    - components/ContactSection.tsx — verify SectionReveal usage, no layout prop, no motion.* imports
    - components/Navbar.tsx — verify no layout prop; any animation should be non-scroll (nav is fixed)
    - components/Footer.tsx — verify no scroll animation or layout prop
    - .planning/phases/05-polish-analytics/05-PATTERNS.md — §Animation Audit Baseline defines the spec; §Audit Targets table shows pre-flight status per component
  </read_first>
  <action>
    Read each component file listed in read_first. For each file, check:
      (a) No `layout` prop anywhere in the JSX
      (b) No direct `motion.*` import — only `m` from 'framer-motion' is allowed in client components
      (c) Any SectionReveal usage uses the component props only (delay, animate) — no custom variant overrides
      (d) No section component calls any animation more than once per viewport entry (once:true is enforced by SectionReveal itself)

    If ALL four checks pass for ALL components: document the results as a table in your working notes and proceed to the next task. No file changes needed.

    If ANY check fails: apply the minimal fix to bring the component into spec compliance before proceeding. The fix must not change visual output for passing components.

    HeroSection hero stagger (D-07): if the 6 staggered SectionReveal wrappers with delays 0–0.3s read as excessive after a fresh review, reduce the stagger count (e.g., to 3–4 items) or tighten the delay range (e.g., 0–0.2s). This is Claude's discretion — only make the change if it genuinely improves the experience.
  </action>
  <verify>
    <automated>grep -rn "layout\b" /Users/yash/Personal/Portfolio/components/ --include="*.tsx" | grep -v "layout\.tsx\|// layout\|RootLayout\|layout-" | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - grep for 'layout' prop in all component TSX files returns no Framer Motion layout prop usage (grep -rn " layout[={]" components/ produces no hits on animation elements)
    - grep for 'motion\.' in all component TSX files returns no direct motion.* import or usage (grep -rn "from 'framer-motion'.*motion\b\|import.*motion\b" components/ produces no hits, only 'm' is imported)
    - SectionReveal.tsx contains 'once: true' — confirmed present
    - All seven section components (Hero, About, Work, Projects, Contact, Navbar, Footer) have been read and checked; audit result documented in summary
    - If any file was changed: `cd /Users/yash/Personal/Portfolio && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    All section components pass the animation spec audit. Any failures found were fixed. Results documented in summary.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lighthouse run and Vercel dashboard activation</name>
  <what-built>
    Task 1 completed the animation audit. The site has Analytics + SpeedInsights wired (Plan 01) and all animation compliance verified (Task 1). The production site at https://yashkadam.com should now be running the latest deployment.
  </what-built>
  <how-to-verify>
    Step 1 — Enable Vercel Analytics in your Vercel dashboard (if not already done):
      a. Open https://vercel.com/dashboard → select the yashkadam.com project
      b. Go to the "Analytics" tab in the left sidebar → click "Enable"
      c. Go to the "Speed Insights" tab in the left sidebar → click "Enable"
      (These are one-time clicks per project. If already showing data, skip this step.)

    Step 2 — Run Lighthouse against the deployed production URL:
      Option A (Chrome DevTools — recommended):
        a. Open Chrome and navigate to https://yashkadam.com
        b. Open DevTools → Lighthouse tab
        c. Set: Mode = Navigation, Device = Mobile, Categories = Performance only
        d. Click "Analyze page load"
        e. When done, note the LCP, CLS, and INP values from the report

      Option B (CLI — if Chrome DevTools is not convenient):
        npx lighthouse https://yashkadam.com \
          --output=json,html \
          --output-path=./lighthouse-report \
          --only-categories=performance \
          --chrome-flags="--headless"
        Then open lighthouse-report.report.html in a browser

    Step 3 — Record the three values:
      LCP: _____ (pass if < 2.5s)
      CLS: _____ (pass if < 0.1)
      INP: _____ (pass if < 200ms)

    Step 4 — Assess pass/fail:
      If ALL THREE PASS: Type "approved: LCP=X CLS=Y INP=Z" with the measured values
      If ANY FAIL: Describe which metric failed and its value (e.g., "LCP failed at 3.2s"). Claude will diagnose and fix in a follow-up task.

    IMPORTANT: Run Lighthouse against https://yashkadam.com (production), NOT http://localhost:3000. The dev server lacks CDN optimization and gives misleading results.
  </how-to-verify>
  <resume-signal>
    If all pass: "approved: LCP=X CLS=Y INP=Z"
    If any fail: "failed: [metric]=[value], [metric]=[value], ..." — describe what Lighthouse flagged as the cause
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Vercel CDN | Lighthouse measures performance of the page as delivered from Vercel Edge; CWV are browser-side metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Framer Motion layout prop (CLS source) | accept | Audit confirms no layout prop exists in any component; no code change needed; CLS from animation is already mitigated by existing SectionReveal design |
| T-05-04 | Information Disclosure | Lighthouse report output (./lighthouse-report.*) | accept | Report is local dev artifact; contains only performance data for a public URL; no secrets; .gitignore can exclude if desired |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; audit-only phase |
</threat_model>

<verification>
After the checkpoint returns "approved":
1. Record LCP, CLS, INP from the Lighthouse run in the phase summary
2. Confirm: LCP < 2.5s, CLS < 0.1, INP < 200ms — all three must be true
3. Confirm: Vercel Analytics tab in the dashboard is enabled (human verified)
4. Confirm: Vercel Speed Insights tab in the dashboard is enabled (human verified)
5. If any CWV fix was applied: `cd /Users/yash/Personal/Portfolio && npx next build` exits 0
</verification>

<success_criteria>
- Animation audit passed: no layout prop, no motion.* imports, all sections animate once per viewport entry
- Lighthouse reports LCP < 2.5s on https://yashkadam.com
- Lighthouse reports CLS < 0.1 on https://yashkadam.com
- Lighthouse reports INP < 200ms on https://yashkadam.com
- Vercel Analytics dashboard is enabled and receiving pageview events
- Vercel Speed Insights dashboard is enabled and receiving CWV data from real users
</success_criteria>

<output>
Create `.planning/phases/05-polish-analytics/05-02-SUMMARY.md` when done. Include the Lighthouse CWV values (LCP, CLS, INP) and animation audit results in the summary.
</output>
