---
phase: 02-portfolio-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/globals.css
  - components/Navbar.tsx
  - components/Footer.tsx
  - app/blog/page.tsx
autonomous: true
requirements: [PORT-05]

must_haves:
  truths:
    - "Navbar shows Home link with indigo-400 underline + text-accent when visiting '/'"
    - "Navbar shows Blog link with indigo-400 underline + text-accent when visiting '/blog'"
    - "Footer LinkedIn link has no TODO comment and href is confirmed with user"
    - "app/globals.css :root contains --color-accent: #818cf8 and --color-accent-hover: #6366f1"
    - "app/layout.tsx imports Geist and Geist_Mono (not Inter and JetBrains_Mono)"
    - "Visiting /blog shows a 'Blog posts coming soon.' message instead of a 404"
    - "next build exits 0 with zero TypeScript errors"
  artifacts:
    - path: "app/globals.css"
      provides: "Updated CSS design tokens"
      contains: "--color-accent: #818cf8"
    - path: "app/layout.tsx"
      provides: "Root layout with Geist font swap"
      contains: "Geist_Mono"
    - path: "components/Navbar.tsx"
      provides: "Client Component Navbar with active link state"
      contains: "usePathname"
    - path: "components/Footer.tsx"
      provides: "Footer with confirmed LinkedIn URL, no TODO"
    - path: "app/blog/page.tsx"
      provides: "Blog stub page preventing 404"
      contains: "coming soon"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS custom properties consumed by font-sans and font-mono"
      pattern: "--font-sans.*Geist"
    - from: "components/Navbar.tsx"
      to: "lib/utils.ts"
      via: "cn() import for conditional active-link classes"
      pattern: "from '@/lib/utils'"
---

<objective>
Update the design system foundation for Phase 2: swap Geist fonts, install indigo-400 accent color, wire Navbar active-link state (D-02, D-03), resolve the WR-04 LinkedIn TODO in Footer, and add the blog stub page to prevent the WR-01 404.

Purpose: Every section component built in Plans 02-02 and 02-03 inherits the Geist font and indigo accent automatically via CSS custom properties. Navbar active state and Footer LinkedIn fix are small surgical changes that must be done before the page goes live.

Output: Updated layout.tsx (font swap), globals.css (accent + font-fallback values), Navbar.tsx (usePathname active-link state), Footer.tsx (confirmed LinkedIn URL), app/blog/page.tsx (coming-soon stub).
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-page/02-CONTEXT.md
@.planning/phases/02-portfolio-page/02-RESEARCH.md
@.planning/phases/02-portfolio-page/02-PATTERNS.md
@.planning/phases/02-portfolio-page/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Font swap and accent color update (layout.tsx + globals.css)</name>
  <files>app/layout.tsx, app/globals.css</files>
  <read_first>
    - app/layout.tsx — read the full file; identify the Inter/JetBrains_Mono import block (lines ~5-22) and the html className spread; do NOT touch FOUC script, LazyMotion, skip link, Navbar, Footer imports, or body class
    - app/globals.css — read the full file; identify the :root block with --color-accent and --color-accent-hover; do NOT touch --color-bg, --color-surface, --color-text, --color-text-muted, --color-border, --color-destructive
    - .planning/phases/02-portfolio-page/02-PATTERNS.md §"app/layout.tsx" and §"app/globals.css" — exact before/after diffs
  </read_first>
  <action>
    In app/layout.tsx: replace the import { Inter, JetBrains_Mono } from 'next/font/google' block with import { Geist, Geist_Mono } from 'next/font/google'. Rename the const variables: inter → geist with variable '--font-sans', and jetbrainsMono → geistMono with weight ['400','600'] and variable '--font-mono'. Update the html element's className spread from ${inter.variable} ${jetbrainsMono.variable} to ${geist.variable} ${geistMono.variable}. Preserve suppressHydrationWarning. Also update the metadata export (if present in layout.tsx) title and description to 'Yash Kadam — Technical Lead' and 'Technical Lead and fullstack developer. I write code, lead teams, and ship things.' — but do NOT add OG/twitter metadata to layout.tsx; that belongs in app/page.tsx (Plan 02-04). Do NOT touch the FOUC script, LazyMotion, Navbar, Footer imports, or body className.

    In app/globals.css: in the :root block, change --color-accent from #22d3ee to #818cf8 and --color-accent-hover from #06b6d4 to #6366f1. Also update --font-sans fallback string to 'Geist', system-ui, -apple-system, sans-serif and --font-mono fallback to 'Geist Mono', 'Fira Code', ui-monospace, monospace. Touch only these four property values — all other :root tokens stay unchanged.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && grep -c "Geist_Mono" app/layout.tsx && grep -c "#818cf8" app/globals.css && grep -c "#6366f1" app/globals.css && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - app/layout.tsx imports Geist and Geist_Mono (not Inter/JetBrains_Mono); html className uses geist.variable and geistMono.variable
    - app/globals.css :root has --color-accent: #818cf8 and --color-accent-hover: #6366f1
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Confirm LinkedIn URL and work history content</name>
  <what-built>Nothing automated yet — this task collects required user content before ContactSection and WorkSection can be implemented in Plans 02-02 and 02-03.</what-built>
  <how-to-verify>
    Ask the user for the following before proceeding. Record answers as they will be used verbatim in components/Footer.tsx (plan 02-01), components/WorkSection.tsx (plan 02-03), and components/ContactSection.tsx (plan 02-03):

    1. LinkedIn URL — Is "https://linkedin.com/in/yashkadam" your correct LinkedIn profile URL? If not, provide the correct slug.

    2. Work History — For each previous role (1-2 roles prior to KingsleyGate), provide:
       - Company name
       - Job title
       - Date range (e.g. "Jan 2021 – Mar 2023")
       - One impact sentence (max ~15 words — what you shipped or changed)
       KingsleyGate / Technical Lead is already known. Only previous roles are needed here.

    3. Project live URLs — Provide the live deployment URL for:
       - Nekomori (the live site URL, not GitHub)
       - Brew Index (the live site URL, not GitHub)
       And the exact GitHub repo slugs: github.com/yash-278/??? for each.

    4. Hero photo — Drop your photo file at public/yash.jpg (or tell the executor what filename you will use; default is yash.jpg).
  </how-to-verify>
  <resume-signal>Type "content ready" once you've answered the above. Paste answers inline so the executor can reference them in the next tasks.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Navbar active-link state + Footer LinkedIn fix + blog stub</name>
  <files>components/Navbar.tsx, components/Footer.tsx, app/blog/page.tsx</files>
  <read_first>
    - components/Navbar.tsx — read the full file; note the existing Link imports, className strings, and the TODO Phase 2 comment for active state; the file is currently a Server Component with no 'use client'
    - components/Footer.tsx — read the full file; find line with TODO comment about LinkedIn URL and the href="https://linkedin.com/in/yashkadam" anchor
    - .planning/phases/02-portfolio-page/02-PATTERNS.md §"components/Navbar.tsx" and §"components/Footer.tsx" — exact minimal diffs
    - lib/utils.ts — confirm cn() export signature before importing
  </read_first>
  <action>
    In components/Navbar.tsx: add 'use client' directive as the very first line (before any imports). Add import { usePathname } from 'next/navigation' and import { cn } from '@/lib/utils' to the existing imports. Inside the component function body (before the return), add const pathname = usePathname(). Replace both nav Link elements' className props with cn()-based conditional: base classes are 'text-sm font-semibold transition-colors duration-150 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent'; active condition (pathname === '/' for Home, pathname === '/blog' for Blog) applies 'text-accent border-b-2 border-accent pb-0.5'; inactive applies 'text-text hover:text-accent'. Add aria-current={pathname === href ? 'page' : undefined} to each Link. Do NOT restructure the component — only add the three lines and swap className props. Keep aria-label="Main navigation" on the nav element.

    In components/Footer.tsx: remove the {/* TODO: confirm LinkedIn URL slug */} comment entirely. Update the href of the LinkedIn anchor with the URL confirmed by the user in Task 2. If the user confirmed linkedin.com/in/yashkadam is correct, simply remove the TODO comment; if they gave a different slug, update the href accordingly.

    Create app/blog/page.tsx as a new file with a Server Component (no 'use client'). Content: a page with a div using className="mx-auto max-w-5xl px-6 py-24" containing a p tag with className="text-text-muted text-base" and text "Blog posts coming soon." — matching the container/layout pattern from the existing section components. Export default function BlogPage().
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && grep -c "usePathname" components/Navbar.tsx && grep -c "'use client'" components/Navbar.tsx && grep -v "TODO" components/Footer.tsx | grep -c "linkedin.com" && test -f app/blog/page.tsx && echo "blog page exists" && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - components/Navbar.tsx has 'use client' as first line, imports usePathname and cn, applies active-link conditional classes per D-03 (indigo-400 underline)
    - components/Footer.tsx has no TODO comment; LinkedIn href is the user-confirmed URL
    - app/blog/page.tsx exists and exports BlogPage with "Blog posts coming soon." text
    - npx tsc --noEmit exits 0
    - next dev shows Navbar with indigo underline on the active route; /blog no longer 404s
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout.tsx → browser | Fonts loaded from Google Fonts CDN via next/font/google; no user input crosses this boundary |
| Footer → external LinkedIn URL | Unverified external URL could point to wrong profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Footer.tsx LinkedIn href | mitigate | Task 2 requires user confirmation of LinkedIn URL before hardcoding; if unconfirmed, omit the link entirely |
| T-02-02 | Tampering | Navbar.tsx 'use client' boundary | accept | usePathname is a read-only Next.js hook; no user input; no write operations; safe Client Component |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 02-01; all packages are Phase 1 verified |
</threat_model>

<verification>
After all three tasks complete:
- Run: cd /Users/yash/Personal/Portfolio && npm run build (or next build) — must exit 0 with zero TypeScript errors
- Confirm / route shows as static (○) in build output
- Confirm /blog route shows as static (○) in build output
- Run next dev and visit http://localhost:3000 — Navbar Home link should show indigo underline
- Visit http://localhost:3000/blog — should show "Blog posts coming soon." text, not a 404
</verification>

<success_criteria>
- app/globals.css :root has --color-accent: #818cf8 (indigo-400, per D-03)
- app/layout.tsx imports Geist + Geist_Mono (per D-02); html className spreads both variables
- components/Navbar.tsx is a 'use client' component; usePathname drives aria-current and active indigo underline on the current route
- components/Footer.tsx has confirmed LinkedIn URL with zero TODO comments (resolves WR-04)
- app/blog/page.tsx exists and prevents the WR-01 404
- next build exits 0; zero TypeScript errors
</success_criteria>

<output>
Create .planning/phases/02-portfolio-page/02-01-SUMMARY.md when done
</output>
