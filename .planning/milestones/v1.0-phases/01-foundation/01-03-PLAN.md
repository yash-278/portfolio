---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - components/Navbar.tsx
  - components/Footer.tsx
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-05
must_haves:
  truths:
    - "No white flash (FOUC) on page load in a browser with dark mode preference — dark class is on <html> before first paint"
    - "D-04: inline blocking script in app/layout.tsx adds dark class to <html> before first paint — no next-themes, no system preference detection, no toggle UI"
    - "LazyMotion wraps <main> in root layout with features={domAnimation} strict — no motion.* import anywhere in the codebase"
    - "D-02: BackgroundGradientAnimation is absent — not imported anywhere in app/ or components/"
    - "Navbar is visible on every page with logo 'Yash Kadam', nav links 'Home' and 'Blog'"
    - "D-06: Navbar contains exactly logo/name plus Home and Blog links — no About/Projects anchors, no hamburger menu"
    - "Footer is visible on every page with name, copyright, and GitHub/LinkedIn/Twitter icon links"
    - "D-07: Footer contains Yash Kadam name, copyright, and social links to github.com/yash-278, LinkedIn, and twitter.com/yashkadam278"
    - "app/layout.tsx has no 'use client' directive — it is a Server Component"
    - "next build exits 0 after this plan"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with FOUC script, fonts, LazyMotion, Navbar, Footer"
      contains: "dangerouslySetInnerHTML"
    - path: "components/Navbar.tsx"
      provides: "Fixed top navigation"
      contains: "aria-label=\"Main navigation\""
    - path: "components/Footer.tsx"
      provides: "Static footer with social icons"
      contains: "github.com/yash-278"
    - path: "app/page.tsx"
      provides: "Home placeholder page"
  key_links:
    - from: "app/layout.tsx"
      to: "components/Navbar.tsx"
      via: "import Navbar from '@/components/Navbar'"
      pattern: "import Navbar"
    - from: "app/layout.tsx"
      to: "framer-motion LazyMotion"
      via: "import { LazyMotion, domAnimation } from 'framer-motion'"
      pattern: "LazyMotion"
    - from: "app/layout.tsx"
      to: "<html> dark class"
      via: "dangerouslySetInnerHTML inline blocking script"
      pattern: "dangerouslySetInnerHTML"
---

<objective>
Build the root layout shell with the FOUC prevention script (per D-04), LazyMotion + domAnimation wrapper (per FOUND-03), and the two shared layout components: Navbar (per D-06) and Footer (per D-07). Replace the scaffold placeholder home page with a minimal Phase 1 stub.

Purpose: This plan delivers the complete "visible skeleton" — a real browser visit will show the dark-mode Navbar and Footer, with zero white flash, and the entire page tree is animation-ready via LazyMotion. This is the core walking skeleton deliverable.

Output: app/layout.tsx (complete), components/Navbar.tsx, components/Footer.tsx, app/page.tsx (placeholder).
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build root layout with FOUC script, fonts, and LazyMotion</name>
  <files>app/layout.tsx, app/page.tsx</files>

  <read_first>
    - app/layout.tsx (current scaffold content — understand what to replace)
    - app/page.tsx (current scaffold content — understand what to replace)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Dark Mode FOUC Prevention, Pattern 3: LazyMotion wrapper, Pattern 4: Font Variables, Code Examples "Root Layout complete Phase 1 skeleton", Pitfall 2: FOUC script placement, Pitfall 3: motion.div leaking, Pitfall 6: use client on layout)
    - .planning/phases/01-foundation/01-PATTERNS.md (app/layout.tsx section — full JSX pattern, font loading pattern, metadata pattern)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Global Layout Contract — body classes, main pt-14, max-w-5xl container)
  </read_first>

  <action>
    FILE 1 — app/layout.tsx:
    Replace the scaffold layout with the Phase 1 root layout. The file must NOT have `'use client'` at the top — it is a Server Component.

    Required imports (in this order):
    - `import type { Metadata } from 'next'`
    - `import { Inter, JetBrains_Mono } from 'next/font/google'`
    - `import { LazyMotion, domAnimation } from 'framer-motion'`
    - `import Navbar from '@/components/Navbar'`
    - `import Footer from '@/components/Footer'`
    - `import './globals.css'`

    Font declarations (after imports):
    - `inter`: subsets `['latin']`, variable `'--font-sans'`, display `'swap'`
    - `jetbrainsMono`: subsets `['latin']`, weight `['400', '600']`, variable `'--font-mono'`, display `'swap'`

    Metadata export:
    - `title: 'Yash Kadam'`
    - `description: 'Technical Lead and fullstack developer'`

    JSX structure (the FOUC script placement is critical):
    - `<html lang="en">` with className spreading both font variables: `${inter.variable} ${jetbrainsMono.variable}`
    - Inside `<head>`: `<script dangerouslySetInnerHTML={{ __html: "document.documentElement.classList.add('dark')" }} />` — this MUST be the FIRST and ONLY direct child of `<head>`, before any stylesheet
    - `<body className="bg-bg text-text font-sans antialiased">`
    - First child of `<body>`: skip link — `<a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:z-50 focus:px-4 focus:py-2 focus:text-accent">Skip to main content</a>`
    - Then `<Navbar />`
    - Then `<LazyMotion features={domAnimation} strict>` wrapping `<main id="main-content" className="pt-14">{children}</main>`
    - Then `<Footer />`

    FILE 2 — app/page.tsx:
    Replace scaffold content with the Phase 1 placeholder. No imports needed. Default export returns `<div className="min-h-screen" />`. No `'use client'` directive — it is a Server Component. Do NOT import HomeHero or BackgroundGradientAnimation (per D-02).
  </action>

  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && grep -c "dangerouslySetInnerHTML" app/layout.tsx && grep -c "LazyMotion" app/layout.tsx && grep -v "^//" app/layout.tsx | grep -c "use client" || true</automated>
  </verify>

  <acceptance_criteria>
    - app/layout.tsx contains `dangerouslySetInnerHTML` (FOUC script present)
    - app/layout.tsx contains `LazyMotion` with `features={domAnimation}` and `strict`
    - app/layout.tsx does NOT contain `'use client'` directive
    - app/layout.tsx contains `import { Inter, JetBrains_Mono } from 'next/font/google'`
    - app/layout.tsx contains `import { LazyMotion, domAnimation } from 'framer-motion'` (not from 'motion/react')
    - app/layout.tsx contains `<main id="main-content" className="pt-14">`
    - app/layout.tsx contains `href="#main-content"` (skip link)
    - app/layout.tsx `<body>` element has className including `bg-bg`, `text-text`, `font-sans`, `antialiased`
    - app/page.tsx does NOT import HomeHero or BackgroundGradientAnimation
    - app/page.tsx does NOT have `'use client'`
    - No file in components/ or app/ contains `import { motion }` from 'framer-motion' (motion.* is forbidden)
  </acceptance_criteria>

  <done>Root layout complete with synchronous FOUC script in head, Inter + JetBrains Mono via next/font, LazyMotion strict wrapper around main, skip link for accessibility, and placeholder home page.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Navbar and Footer Server Components</name>
  <files>components/Navbar.tsx, components/Footer.tsx</files>

  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Navbar Contract — all specs, Footer Contract — all specs, Accessibility Contract, Copywriting Contract)
    - .planning/phases/01-foundation/01-PATTERNS.md (components/Navbar.tsx "Implementation pattern", components/Footer.tsx "Implementation pattern", "target='_blank' Security Pattern", "Focus Visible Ring", "Server Component Default")
    - .planning/phases/01-foundation/01-RESEARCH.md (security notes on target="_blank" + rel="noopener noreferrer")
  </read_first>

  <action>
    Create components/ directory if it does not exist.

    FILE 1 — components/Navbar.tsx:
    Server Component — no `'use client'`, no `motion.*` imports.

    Required imports:
    - `import Link from 'next/link'`
    (No `cn` import needed if no conditional class logic — use static class strings.)

    JSX structure:
    - Root: `<header className="fixed top-0 left-0 right-0 z-40 h-14 border-b border-border bg-surface backdrop-blur-sm">`
    - Inside: `<nav aria-label="Main navigation" className="mx-auto flex h-full max-w-5xl items-center justify-between px-6">`
    - Left: `<Link href="/" className="text-sm font-semibold text-text transition-colors duration-150 hover:text-accent focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent">Yash Kadam</Link>`
    - Right: `<div className="flex items-center gap-6">` containing two `<Link>` elements:
      - `<Link href="/" ...>Home</Link>` — same className as logo link above
      - `<Link href="/blog" ...>Blog</Link>` — same className

    Active link state: Keep as a TODO comment in the file — `{/* TODO Phase 2: add usePathname() for active link accent + border-b-2 border-accent */}`. Do NOT add `'use client'` or usePathname in Phase 1 — active state is deferred per PATTERNS.md note.

    FILE 2 — components/Footer.tsx:
    Server Component — no `'use client'`, no `motion.*` imports.

    Required imports:
    - `import { Github, Linkedin, Twitter } from 'lucide-react'`

    JSX structure:
    - Root: `<footer className="border-t border-border bg-surface">`
    - Inside: `<div className="mx-auto flex max-w-5xl flex-col items-start justify-between gap-4 px-6 py-8 sm:flex-row sm:items-center">`
    - Left column: `<div>` containing:
      - `<p className="text-base text-text">Yash Kadam</p>`
      - `<p className="text-sm text-text-muted">© 2025 Yash Kadam</p>`
    - Right column: `<div className="flex items-center gap-2">` containing 3 icon link `<a>` elements:
      - GitHub: `href="https://github.com/yash-278"`, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label="GitHub profile"`, className includes `min-h-11 min-w-11 flex items-center justify-center p-2 text-text-muted transition-colors duration-150 hover:text-accent focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`. Icon: `<Github size={20} aria-hidden="true" />`
      - LinkedIn: `href="https://linkedin.com/in/yashkadam"` (with `{/* TODO: confirm LinkedIn URL slug */}` inline comment), `target="_blank"`, `rel="noopener noreferrer"`, `aria-label="LinkedIn profile"`, same className. Icon: `<Linkedin size={20} aria-hidden="true" />`
      - Twitter/X: `href="https://twitter.com/yashkadam278"`, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label="X (Twitter) profile"`, same className. Icon: `<Twitter size={20} aria-hidden="true" />`

    All three social links MUST have both `target="_blank"` AND `rel="noopener noreferrer"` — required to prevent tab-napping (per RESEARCH.md security notes and UI-SPEC accessibility contract).
  </action>

  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && grep -c "aria-label=\"Main navigation\"" components/Navbar.tsx && grep -c "noopener noreferrer" components/Footer.tsx && grep -c "github.com/yash-278" components/Footer.tsx</automated>
  </verify>

  <acceptance_criteria>
    - components/Navbar.tsx contains `aria-label="Main navigation"` on the `<nav>` element
    - components/Navbar.tsx contains `href="/"` (Home link) and `href="/blog"` (Blog link)
    - components/Navbar.tsx contains `className` with `fixed top-0 left-0 right-0 z-40 h-14` on the header
    - components/Navbar.tsx does NOT contain `'use client'` or `motion` imports
    - components/Footer.tsx contains `rel="noopener noreferrer"` on all three social links (grep count >= 3)
    - components/Footer.tsx contains `href="https://github.com/yash-278"` (exact URL)
    - components/Footer.tsx contains `href="https://twitter.com/yashkadam278"` (exact URL)
    - components/Footer.tsx contains `aria-label="GitHub profile"`, `aria-label="LinkedIn profile"`, `aria-label="X (Twitter) profile"`
    - components/Footer.tsx contains `aria-hidden="true"` on all three lucide icon elements
    - components/Footer.tsx contains `min-h-11 min-w-11` on social link wrappers (44px touch target per UI-SPEC)
    - components/Footer.tsx does NOT contain `'use client'` or `motion` imports
    - `npm run build` (or `npx next build`) exits 0
  </acceptance_criteria>

  <done>Navbar (fixed, dark, logo + 2 links, accessible) and Footer (static, 3 social icons with noopener, WCAG touch targets) are both Server Components visible on every page. next build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout.tsx inline script → browser DOM | Script runs synchronously before paint; content is hardcoded |
| Footer social links → external sites | External links use target="_blank" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | XSS | `dangerouslySetInnerHTML` in FOUC script | accept | Script content is the static string `"document.documentElement.classList.add('dark')"` — no user input is interpolated now or ever; XSS risk is nil. Adding any dynamic value to this string is forbidden. |
| T-03-02 | Tampering | Tab-napping via social links | mitigate | All three Footer social links MUST include `rel="noopener noreferrer"` alongside `target="_blank"` — prevents malicious page from accessing `window.opener` |
| T-03-03 | Information Disclosure | LinkedIn URL TODO comment | accept | Comment is developer-facing only; not user-visible |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new installs in this plan; all packages installed and verified in Plan 01 |

</threat_model>

<verification>
After both tasks complete, run:

1. Build check: `npx next build` — must exit 0 with zero TypeScript errors
2. FOUC script: `grep "dangerouslySetInnerHTML" app/layout.tsx` — must match
3. LazyMotion: `grep "LazyMotion.*strict" app/layout.tsx` — must match
4. No motion.* imports: `grep -r "import { motion" components/ app/` — must return nothing
5. use client absent from layout: `grep -v "^//" app/layout.tsx | grep "use client"` — must return nothing
6. Tab-napping guard: `grep -c "noopener noreferrer" components/Footer.tsx` — must be >= 3
7. Dev server smoke: `npx next dev` starts without error; visit http://localhost:3000 shows Navbar + Footer on dark background
</verification>

<success_criteria>
- app/layout.tsx: FOUC script as first head child, Inter + JetBrains Mono loaded, LazyMotion strict wrapper, Navbar + Footer imported, skip link present
- components/Navbar.tsx: fixed header, aria-label="Main navigation", Home + Blog links, hover:text-accent, no use client
- components/Footer.tsx: Yash Kadam + copyright + 3 social icons, all with noopener noreferrer, 44px touch targets, aria-labels
- app/page.tsx: minimal placeholder, no use client, no BackgroundGradientAnimation
- next build exits 0
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
