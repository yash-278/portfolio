---
phase: 02-portfolio-page
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/SectionReveal.tsx
  - components/HeroSection.tsx
  - components/AboutSection.tsx
  - app/page.tsx
  - public/yash.jpg
autonomous: false
requirements: [PORT-01, PORT-02]

must_haves:
  truths:
    - "Visiting http://localhost:3000 shows a hero split-layout: 'Yash Kadam' at text-5xl on the left, photo on the right"
    - "Hero pitch line reads exactly: 'I write code, lead teams, and ship things.' (per D-05)"
    - "Hero name, title, pitch, and photo each animate in on mount (opacity 0→1, y 24→0) with 0.1s stagger (per D-06)"
    - "Hero 'View my work →' anchor scrolls to #projects; 'GitHub' opens https://github.com/yash-278 in a new tab"
    - "About section shows 2-3 sentences covering KingsleyGate role, background, and Go focus (per D-13)"
    - "Both Hero and About sections are visible without scrolling hint — above the fold or immediately below hero"
    - "SectionReveal renders using m.* from 'framer-motion' — never motion.*"
    - "HeroSection and AboutSection have no 'use client' directive — they are Server Components"
    - "next build exits 0; / route is marked ○ (static) in build output"
  artifacts:
    - path: "components/SectionReveal.tsx"
      provides: "'use client' animation leaf wrapping m.div with scroll/mount modes"
      exports: ["default SectionReveal"]
      contains: "'use client'"
    - path: "components/HeroSection.tsx"
      provides: "Hero split-layout Server Component"
      contains: "id=\"hero\""
    - path: "components/AboutSection.tsx"
      provides: "About prose Server Component"
      contains: "id=\"about\""
    - path: "app/page.tsx"
      provides: "Home page assembling Hero + About sections (Work/Projects/Contact added in 02-03)"
    - path: "public/yash.jpg"
      provides: "Hero photo asset"
  key_links:
    - from: "components/HeroSection.tsx"
      to: "components/SectionReveal.tsx"
      via: "SectionReveal animate='mount' wrapper"
      pattern: "animate=\"mount\""
    - from: "components/AboutSection.tsx"
      to: "components/SectionReveal.tsx"
      via: "SectionReveal default scroll wrapper"
      pattern: "<SectionReveal"
    - from: "app/page.tsx"
      to: "components/HeroSection.tsx"
      via: "Server Component import"
      pattern: "HeroSection"
---

<objective>
Build the SectionReveal animation primitive and the first two portfolio sections (Hero + About), then wire them into app/page.tsx so the home page shows real content. This is the first vertical slice: a visitor opening the site sees the hero split-layout and about prose in a single dev-server load.

Purpose: Delivers PORT-01 (Hero) and PORT-02 (About). Validates the SectionReveal pattern before WorkSection/ProjectsSection/ContactSection are built in Plan 02-03.

Output: components/SectionReveal.tsx (Client Component animation leaf), components/HeroSection.tsx (Server Component), components/AboutSection.tsx (Server Component), updated app/page.tsx assembling both sections, and public/yash.jpg (user-provided photo).
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-page/02-CONTEXT.md
@.planning/phases/02-portfolio-page/02-RESEARCH.md
@.planning/phases/02-portfolio-page/02-PATTERNS.md
@.planning/phases/02-portfolio-page/02-UI-SPEC.md
@.planning/phases/02-portfolio-page/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Hero photo placement</name>
  <what-built>Nothing automated — this task collects the hero photo asset before HeroSection can render it.</what-built>
  <how-to-verify>
    Ask the user to drop their hero photo into the project's public/ directory as public/yash.jpg. The file must be:
    - Filename: yash.jpg (or confirm a different filename if user prefers — update src="/yash.jpg" in HeroSection.tsx accordingly)
    - Format: JPEG or PNG (next/image handles both)
    - Minimum dimensions: 640×640px recommended for a 320px display size with 2x retina

    If the user prefers a different filename, note the filename to use in the HeroSection src prop.
  </how-to-verify>
  <resume-signal>Type "photo ready" and confirm the filename (e.g. "photo ready, yash.jpg") once the file is in public/.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SectionReveal Client Component</name>
  <files>components/SectionReveal.tsx</files>
  <read_first>
    - app/layout.tsx — confirm LazyMotion wraps only the main element (the framer-motion boundary); do NOT move or modify LazyMotion; SectionReveal will render inside main and is within the boundary
    - .planning/phases/02-portfolio-page/02-PATTERNS.md §"components/SectionReveal.tsx" — full implementation spec including the animate prop ('scroll' | 'mount') and the conditional motionProps logic
    - .planning/phases/02-portfolio-page/02-UI-SPEC.md §"Animation Contract" — hard constraints: opacity + y only, viewport once:true margin:-80px, duration 0.5s easeOut
  </read_first>
  <action>
    Create components/SectionReveal.tsx as a new file. It must be a 'use client' directive at the very top line. Import { m } from 'framer-motion' — never import { motion } from 'framer-motion' (LazyMotion strict mode will throw at runtime). Define a variants object with hidden: { opacity: 0, y: 24 } and visible: { opacity: 1, y: 0 }. Define the SectionRevealProps interface with children: React.ReactNode, delay?: number (default 0), and animate?: 'scroll' | 'mount' (default 'scroll'). Inside the component: when animate === 'mount', motionProps is { initial: 'hidden', animate: 'visible' }; otherwise motionProps is { initial: 'hidden', whileInView: 'visible', viewport: { once: true, margin: '-80px' } }. Return m.div with variants={variants}, spread motionProps, and transition={{ duration: 0.5, ease: 'easeOut', delay }}. Do NOT add layout prop, scale, rotate, or blur to the animation. Use only opacity and y.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && test -f components/SectionReveal.tsx && grep -c "'use client'" components/SectionReveal.tsx && grep -c "from 'framer-motion'" components/SectionReveal.tsx && grep -c "motion\." components/SectionReveal.tsx || echo "no motion.* found (correct)" && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - components/SectionReveal.tsx exists with 'use client' as first line
    - imports { m } from 'framer-motion' — no import of { motion } anywhere in the file
    - animate prop accepts 'scroll' (default) and 'mount' modes
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: HeroSection + AboutSection + wire app/page.tsx</name>
  <files>components/HeroSection.tsx, components/AboutSection.tsx, app/page.tsx</files>
  <read_first>
    - app/page.tsx — read the current file (a minimal Phase 1 placeholder); it will be replaced
    - components/Navbar.tsx — read to confirm the Server Component pattern (no 'use client', import Link from 'next/link') to replicate for HeroSection and AboutSection
    - components/Footer.tsx — read to confirm the max-w-5xl px-6 mx-auto container pattern and the inline SVG/link pattern for external links
    - .planning/phases/02-portfolio-page/02-PATTERNS.md §"components/HeroSection.tsx" and §"components/AboutSection.tsx" — full structural spec with exact Tailwind classes
    - .planning/phases/02-portfolio-page/02-UI-SPEC.md §"Hero Section Contract" and §"About Section Contract" — layout dimensions, copy, and color token usage
    - .planning/phases/02-portfolio-page/02-CONTEXT.md §"Decisions" — D-04 (split layout), D-05 (verbatim pitch line), D-06 (hero content), D-13 (about prose content)
    - .planning/phases/02-portfolio-page/02-01-SUMMARY.md — confirm font swap and accent color are in place before building components that depend on them
  </read_first>
  <action>
    Create components/HeroSection.tsx as a Server Component (no 'use client'). Import Image from 'next/image' and SectionReveal from '@/components/SectionReveal'. The section element has id="hero", aria-labelledby="hero-heading", className="py-20 md:py-28 scroll-mt-14", and an inline style with backgroundImage: 'radial-gradient(ellipse 80% 50% at 50% -10%, rgba(129, 140, 248, 0.06) 0%, transparent 70%)'. Inner container: div with className="mx-auto flex max-w-5xl flex-col items-center gap-8 px-6 md:flex-row md:items-center md:gap-12". Text column: div with className="flex flex-col md:flex-1". Wrap each text element in SectionReveal with animate="mount" and staggered delays: h1 "Yash Kadam" at delay={0} with className="text-5xl font-semibold leading-tight text-text", id="hero-heading"; p "Technical Lead" at delay={0.1} with className="mt-2 text-2xl text-text-muted"; p "I write code, lead teams, and ship things." at delay={0.2} with className="mt-4 text-2xl text-text". CTA row wrapped in SectionReveal delay={0.3}: div className="mt-8 flex flex-wrap gap-4" containing anchor href="#projects" with className="text-base font-semibold text-accent transition-colors duration-150 hover:text-accent-hover focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent" text "View my work →"; and anchor href="https://github.com/yash-278" target="_blank" rel="noopener noreferrer" with className="text-base text-text-muted transition-colors duration-150 hover:text-text focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent" text "GitHub". Photo column wrapped in SectionReveal animate="mount" delay={0.3}: div className="flex-shrink-0" with Image src="/yash.jpg" (or confirmed filename from Task 1), alt="Yash Kadam", width={320}, height={320}, priority, sizes="(max-width: 768px) 200px, 320px", className="rounded-2xl object-cover ring-1 ring-border". On mobile the photo should be centered — add className="mx-auto" on the photo wrapper div. Use the verbatim pitch line from D-05: "I write code, lead teams, and ship things." — do not paraphrase.

    Create components/AboutSection.tsx as a Server Component (no 'use client'). Import SectionReveal from '@/components/SectionReveal'. Section element: id="about", aria-labelledby="about-heading", className="py-24 scroll-mt-14". Container: div className="mx-auto max-w-5xl px-6". Heading pattern: h2 id="about-heading" className="text-2xl font-semibold text-text" text "About"; then div className="mt-2 mb-10 h-0.5 w-8 bg-accent". Wrap prose in SectionReveal (default scroll mode). Prose container: div className="max-w-2xl" with p className="text-base leading-relaxed text-text" containing 2-3 sentences per D-13: sentence 1 about current role as Technical Lead at KingsleyGate, sentence 2 brief background (how they got here), sentence 3 shifting focus toward backend/Go. Ask the user to confirm the exact prose before writing it — if no instruction given, use placeholder text noting [EXECUTOR: fill About prose per D-13 — current role at KingsleyGate, background, Go focus, max 3 sentences].

    Replace app/page.tsx entirely. Import type { Metadata } from 'next', HeroSection from '@/components/HeroSection', and AboutSection from '@/components/AboutSection'. Add a minimal metadata export with just title and description (full OG/twitter metadata is added in Plan 02-04): title: 'Yash Kadam — Technical Lead', description: 'Technical Lead and fullstack developer. I write code, lead teams, and ship things.'. Export default function Home() returning a fragment with HeroSection and AboutSection. Do NOT add WorkSection, ProjectsSection, or ContactSection yet (those come in Plan 02-03). Do NOT add async, fetch(), or getServerSideProps — the page must remain statically generated.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && test -f components/HeroSection.tsx && test -f components/AboutSection.tsx && grep -c "id=\"hero\"" components/HeroSection.tsx && grep -c "id=\"about\"" components/AboutSection.tsx && grep -c "'use client'" components/HeroSection.tsx || echo "no use client (correct for Server Component)" && grep -c "HeroSection" app/page.tsx && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - components/HeroSection.tsx: no 'use client'; id="hero" with scroll-mt-14; hero heading "Yash Kadam" at text-5xl; pitch "I write code, lead teams, and ship things." verbatim (per D-05); Image has priority prop; all external links have target="_blank" rel="noopener noreferrer"
    - components/AboutSection.tsx: no 'use client'; id="about" with scroll-mt-14; section heading + accent underline pattern; prose text max-w-2xl
    - app/page.tsx imports and renders HeroSection and AboutSection; no async/fetch; metadata export present
    - npx tsc --noEmit exits 0
    - next dev at localhost:3000 shows hero split-layout (text left, photo right on desktop) and about section below
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/yash.jpg → next/image | User-provided binary asset; served by Next.js CDN as optimized image |
| HeroSection → external links | Two outbound links: github.com/yash-278 and #projects anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | HeroSection external links | mitigate | Both external anchors use rel="noopener noreferrer"; no redirect URL — hardcoded trusted domains |
| T-02-04 | Elevation of Privilege | SectionReveal 'use client' boundary | accept | Read-only animation component; no user input; no state mutation; safe Client Component boundary |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 02-02; framer-motion is Phase 1 verified |
</threat_model>

<verification>
After all tasks complete:
- Run: cd /Users/yash/Personal/Portfolio && npm run build — must exit 0, / route must show ○ (static)
- Run next dev — visit http://localhost:3000 — hero split-layout visible (name, title, pitch line, photo, CTAs)
- Hero elements animate in on mount (opacity + y) in staggered sequence
- About section scrolls into view and animates in with scroll trigger
- "View my work →" anchor scrolls page down (links to #projects — no section yet, but anchor should not 404)
- "GitHub" link opens https://github.com/yash-278 in new tab
</verification>

<success_criteria>
- PORT-01: Hero section with name (Yash Kadam), title (Technical Lead), pitch ("I write code, lead teams, and ship things."), GitHub + View my work CTAs — no typewriter animation (per requirements)
- PORT-02: About section with KingsleyGate role, brief background, and Go focus — max 3 sentences
- SectionReveal is the only 'use client' component in this plan; HeroSection and AboutSection are Server Components
- next build exits 0; / route is ○ (static) in build output
</success_criteria>

<output>
Create .planning/phases/02-portfolio-page/02-02-SUMMARY.md when done
</output>
