---
phase: 02-portfolio-page
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - components/WorkSection.tsx
  - components/ProjectsSection.tsx
  - components/ContactSection.tsx
  - app/page.tsx
autonomous: false
requirements: [PORT-03, PORT-04, PORT-05]

must_haves:
  truths:
    - "Work section shows KingsleyGate Technical Lead role + at least one previous role with title, company (in indigo accent color), date range, and one impact sentence per role (per D-07, D-08)"
    - "Projects section shows two cards: Nekomori and Brew Index — each with correct 1-line description (verbatim D-11), tech stack tags, Live ↗ link, and GitHub ↗ link (per D-09, D-10, D-12)"
    - "Contact section shows GitHub, LinkedIn, and Twitter/X links with inline SVG icons — no contact form (per D-14, PORT-05)"
    - "All five sections (hero, about, work, projects, contact) are visible on the home page without a page reload"
    - "All section IDs are: id='hero', id='about', id='work', id='projects', id='contact' with scroll-mt-14 on each"
    - "Project cards use glassmorphism style: bg-surface/60 backdrop-blur-md border-border/60 rounded-2xl"
    - "Work section uses left-border timeline: border-l-2 border-accent/30 pl-6"
    - "All external links (project live, GitHub, social) have target='_blank' rel='noopener noreferrer'"
    - "WorkSection, ProjectsSection, ContactSection are all Server Components (no 'use client')"
    - "next build exits 0; / route is ○ (static) in build output"
  artifacts:
    - path: "components/WorkSection.tsx"
      provides: "Work History Server Component with left-border timeline"
      contains: "id=\"work\""
    - path: "components/ProjectsSection.tsx"
      provides: "Projects Server Component with two glassmorphism cards"
      contains: "id=\"projects\""
    - path: "components/ContactSection.tsx"
      provides: "Contact Server Component with three social icon links"
      contains: "id=\"contact\""
    - path: "app/page.tsx"
      provides: "Complete home page with all five sections assembled"
  key_links:
    - from: "components/WorkSection.tsx"
      to: "components/SectionReveal.tsx"
      via: "SectionReveal with staggered delay per role"
      pattern: "delay=\\{\\d"
    - from: "components/ProjectsSection.tsx"
      to: "components/SectionReveal.tsx"
      via: "SectionReveal with delay={index * 0.1} per card"
      pattern: "delay=\\{0\\."
    - from: "components/ContactSection.tsx"
      to: "components/Footer.tsx"
      via: "SVG paths copied verbatim from Footer.tsx social icons"
      pattern: "aria-label=\"GitHub profile\""
    - from: "app/page.tsx"
      to: "components/WorkSection.tsx"
      via: "Server Component import"
      pattern: "WorkSection"
---

<objective>
Build the Work History, Projects, and Contact sections, then wire all five sections together in app/page.tsx. This is the second vertical slice: after this plan, a visitor can see the complete portfolio page — all five sections — in a single browser load without any page reload.

Purpose: Delivers PORT-03 (Work History), PORT-04 (Projects), and PORT-05 (Contact). Produces the full single-page portfolio that can receive real visitor feedback.

Output: components/WorkSection.tsx, components/ProjectsSection.tsx, components/ContactSection.tsx (all Server Components), and updated app/page.tsx with all five sections imported.
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-page/02-CONTEXT.md
@.planning/phases/02-portfolio-page/02-RESEARCH.md
@.planning/phases/02-portfolio-page/02-PATTERNS.md
@.planning/phases/02-portfolio-page/02-UI-SPEC.md
@.planning/phases/02-portfolio-page/02-01-SUMMARY.md
@.planning/phases/02-portfolio-page/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WorkSection — left-border timeline with user-provided content</name>
  <files>components/WorkSection.tsx</files>
  <read_first>
    - components/Footer.tsx — read to confirm the max-w-5xl px-6 mx-auto container pattern and the bg-surface structure; WorkSection follows the same Server Component pattern
    - components/SectionReveal.tsx — read to confirm the delay prop interface; WorkSection uses delay={0}, delay={0.1}, etc. for staggered role rows
    - .planning/phases/02-portfolio-page/02-PATTERNS.md §"components/WorkSection.tsx" — timeline container, role row pattern, and token usage
    - .planning/phases/02-portfolio-page/02-UI-SPEC.md §"Work History Section Contract" — exact layout spec: border-l-2 border-accent/30 pl-6, gap-8, company name in text-accent
    - .planning/phases/02-portfolio-page/02-CONTEXT.md §"Decisions" D-07 and D-08 — role content spec; previous role content was collected from user in Plan 02-01 Task 2
    - .planning/phases/02-portfolio-page/02-01-SUMMARY.md — find the user-confirmed previous role data (company, title, dates, impact sentence) collected during Plan 02-01
  </read_first>
  <action>
    Create components/WorkSection.tsx as a Server Component (no 'use client'). Import SectionReveal from '@/components/SectionReveal'. Section element: id="work", aria-labelledby="work-heading", className="py-24 scroll-mt-14". Container: div className="mx-auto max-w-5xl px-6". Heading pattern: h2 id="work-heading" className="text-2xl font-semibold text-text" text "Work"; then div className="mt-2 mb-10 h-0.5 w-8 bg-accent". Timeline wrapper: div className="border-l-2 border-accent/30 pl-6" containing div className="flex flex-col gap-8".

    For each role, add a SectionReveal with delay={0} for first role, delay={0.1} for second, delay={0.2} for third (if any). Each role row is a div containing: p className="text-base font-semibold text-text" with job title; p className="text-sm text-text-muted" with span className="text-accent" for company name followed by ' · ' then span for date range; p className="mt-1 text-base text-text" with the one impact sentence.

    Hardcode the following roles (do NOT make them dynamic or fetched):
    - Role 1 (current): KingsleyGate — Technical Lead — dates from user confirmation in 02-01 — impact sentence from user confirmation
    - Role 2 (previous): company name, title, dates, impact sentence from user confirmation in 02-01
    - Role 3 (if provided): company name, title, dates, impact sentence from user confirmation in 02-01

    If the user's previous role content from Plan 02-01 is not available in context, use placeholders matching the exact format: [COMPANY], [TITLE], [DATES], [IMPACT SENTENCE] — make these obvious for the user to find and fill in. Do NOT invent role content.

    Per D-07: no bullet points; per D-08: show KingsleyGate + 1-2 previous roles only.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && test -f components/WorkSection.tsx && grep -c "id=\"work\"" components/WorkSection.tsx && grep -c "border-l-2" components/WorkSection.tsx && grep -c "text-accent" components/WorkSection.tsx && grep -c "'use client'" components/WorkSection.tsx || echo "no use client (correct)" && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - components/WorkSection.tsx exists; no 'use client'; id="work" with scroll-mt-14
    - Left-border timeline: border-l-2 border-accent/30 pl-6
    - KingsleyGate role + at least one previous role rendered; company names in text-accent; dates in text-text-muted
    - One impact sentence per role (or confirmed placeholders if user content not available)
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ProjectsSection — glassmorphism card grid</name>
  <files>components/ProjectsSection.tsx</files>
  <read_first>
    - components/Footer.tsx — read to confirm the Server Component pattern; no 'use client', max-w-5xl mx-auto px-6 container
    - components/SectionReveal.tsx — confirm the delay prop; ProjectsSection wraps each card in SectionReveal with delay={0} and delay={0.1}
    - .planning/phases/02-portfolio-page/02-PATTERNS.md §"components/ProjectsSection.tsx" — full card markup including glassmorphism classes, tech stack tag pattern, and link pattern
    - .planning/phases/02-portfolio-page/02-UI-SPEC.md §"Projects Section Contract" and §"Card Style Contract" — exact card anatomy and all Tailwind values
    - .planning/phases/02-portfolio-page/02-CONTEXT.md §"Decisions" D-09, D-10, D-11, D-12 — project content (verbatim descriptions, tech tags, link URLs)
    - .planning/phases/02-portfolio-page/02-01-SUMMARY.md — find the user-confirmed live URLs and GitHub repo slugs collected during Plan 02-01 Task 2
  </read_first>
  <action>
    Create components/ProjectsSection.tsx as a Server Component (no 'use client'). Import SectionReveal from '@/components/SectionReveal'. Section element: id="projects", aria-labelledby="projects-heading", className="py-24 scroll-mt-14". Container: div className="mx-auto max-w-5xl px-6". Heading pattern: h2 id="projects-heading" className="text-2xl font-semibold text-text" text "Projects"; div className="mt-2 mb-10 h-0.5 w-8 bg-accent". Grid wrapper: div className="grid grid-cols-1 gap-6 md:grid-cols-2".

    Card 1 — Nekomori: wrap in SectionReveal delay={0}. Card element: article className="rounded-2xl border border-border/60 bg-surface/60 p-6 backdrop-blur-md transition-colors duration-200 hover:border-border hover:bg-surface/80". h3 className="text-2xl font-semibold text-text" text "Nekomori". p className="mt-1 text-sm text-text-muted" text "Anime schedule tracker and personal watchlist manager." (verbatim per D-11). Tech tags div className="mt-4 flex flex-wrap gap-2": render ['TypeScript', 'React', 'Node'] as span elements each with className="rounded-md border border-border/80 bg-bg px-2 py-1 font-mono text-sm text-accent". Links div className="mt-4 flex gap-4": anchor href=[user-confirmed Nekomori live URL], target="_blank", rel="noopener noreferrer", aria-label="View Nekomori live site", className="text-sm font-semibold text-text-muted transition-colors duration-150 hover:text-accent focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent" text "Live ↗"; anchor href=[user-confirmed Nekomori GitHub URL e.g. https://github.com/yash-278/nekomori], target="_blank", rel="noopener noreferrer", aria-label="View Nekomori on GitHub", same className, text "GitHub ↗".

    Card 2 — Brew Index: wrap in SectionReveal delay={0.1}. Same card structure. h3 "Brew Index". p "A searchable frontend for the Homebrew package registry." (verbatim per D-11). Tech tags: ['React', 'TypeScript']. Links with user-confirmed Brew Index live URL and GitHub URL.

    If user-confirmed URLs are not available in context from Plan 02-01, use placeholders: href="[NEKOMORI_LIVE_URL]", href="[NEKOMORI_GITHUB_URL]", href="[BREW_INDEX_LIVE_URL]", href="[BREW_INDEX_GITHUB_URL]" — make them obviously placeholder strings. Per D-12: all links must be confirmed before going live.

    Do NOT add more than two project cards (per D-09 — no older demo projects).
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && test -f components/ProjectsSection.tsx && grep -c "id=\"projects\"" components/ProjectsSection.tsx && grep -c "backdrop-blur-md" components/ProjectsSection.tsx && grep -c "Nekomori" components/ProjectsSection.tsx && grep -c "Brew Index" components/ProjectsSection.tsx && grep -c "'use client'" components/ProjectsSection.tsx || echo "no use client (correct)" && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - components/ProjectsSection.tsx exists; no 'use client'; id="projects" with scroll-mt-14
    - Two cards: Nekomori (TypeScript/React/Node) and Brew Index (React/TypeScript) — verbatim descriptions per D-11
    - Glassmorphism style: bg-surface/60 backdrop-blur-md border-border/60 rounded-2xl on each card
    - Each card has Live ↗ and GitHub ↗ links with noopener noreferrer and aria-labels
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ContactSection + complete app/page.tsx with all five sections</name>
  <files>components/ContactSection.tsx, app/page.tsx</files>
  <read_first>
    - components/Footer.tsx — read the FULL file; copy the inline SVG path data verbatim for GitHub (lines ~20-30), LinkedIn (lines ~40-49), and X/Twitter (lines ~58-67); do not re-source SVG paths; copy them exactly from this file
    - app/page.tsx — read the current file (Hero + About from Plan 02-02); this will be updated to add Work, Projects, Contact imports
    - .planning/phases/02-portfolio-page/02-PATTERNS.md §"components/ContactSection.tsx" — contact section structure, social icon link pattern, touch-target classes
    - .planning/phases/02-portfolio-page/02-UI-SPEC.md §"Contact Section Contract" — exact layout, icon labels ("GitHub", "LinkedIn", "X / Twitter"), copy line ("Find me on the internet.")
    - .planning/phases/02-portfolio-page/02-CONTEXT.md D-14 — Contact shows GitHub, LinkedIn, Twitter/X links only; no contact form; GitHub: github.com/yash-278; Twitter/X: @yashkadam278
    - .planning/phases/02-portfolio-page/02-01-SUMMARY.md — find the user-confirmed LinkedIn URL
  </read_first>
  <action>
    Create components/ContactSection.tsx as a Server Component (no 'use client'). Import SectionReveal from '@/components/SectionReveal'. Section element: id="contact", aria-labelledby="contact-heading", className="py-24 scroll-mt-14". Container: div className="mx-auto max-w-5xl px-6". Heading pattern: h2 id="contact-heading" className="text-2xl font-semibold text-text" text "Contact"; div className="mt-2 mb-10 h-0.5 w-8 bg-accent". Wrap remaining content in SectionReveal (default scroll mode). p className="text-base text-text-muted" text "Find me on the internet." (verbatim per UI-SPEC). div className="mt-6 flex flex-wrap gap-4" with three anchor elements:

    GitHub: href="https://github.com/yash-278", target="_blank", rel="noopener noreferrer", aria-label="GitHub profile", className="flex min-h-11 min-w-11 items-center gap-2 p-2 text-text-muted transition-colors duration-150 hover:text-accent focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent". Inside: svg (copy path data verbatim from Footer.tsx GitHub icon) with xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" width={24} height={24}. Then span className="text-sm" text "GitHub".

    LinkedIn: href=[user-confirmed LinkedIn URL from Plan 02-01], target="_blank", rel="noopener noreferrer", aria-label="LinkedIn profile", same className. Inside: svg (copy path data verbatim from Footer.tsx LinkedIn icon). span "LinkedIn".

    X/Twitter: href="https://twitter.com/yashkadam278", target="_blank", rel="noopener noreferrer", aria-label="X (Twitter) profile", same className. Inside: svg (copy path data verbatim from Footer.tsx X/Twitter icon). span "X / Twitter".

    Do NOT add a contact form (PORT-05 explicitly requires no form). Per D-14: three links only.

    Update app/page.tsx: add imports for WorkSection from '@/components/WorkSection', ProjectsSection from '@/components/ProjectsSection', and ContactSection from '@/components/ContactSection'. In the Home function body, add WorkSection, ProjectsSection, and ContactSection after AboutSection in the fragment. The final section order in page.tsx must be: HeroSection, AboutSection, WorkSection, ProjectsSection, ContactSection. Keep the existing minimal metadata export (title + description only — full OG metadata is added in Plan 02-04). Do NOT add async, fetch(), or dynamic imports.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && test -f components/ContactSection.tsx && grep -c "id=\"contact\"" components/ContactSection.tsx && grep -c "github.com/yash-278" components/ContactSection.tsx && grep -c "yashkadam278" components/ContactSection.tsx && grep -c "'use client'" components/ContactSection.tsx || echo "no use client (correct)" && grep -c "WorkSection\|ProjectsSection\|ContactSection" app/page.tsx && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - components/ContactSection.tsx exists; no 'use client'; id="contact" with scroll-mt-14; "Find me on the internet." copy line
    - Three social links: github.com/yash-278, confirmed LinkedIn URL, twitter.com/yashkadam278 — inline SVG paths copied from Footer.tsx
    - All social links have min-h-11 min-w-11 touch targets, noopener noreferrer, and descriptive aria-labels
    - app/page.tsx imports and renders all five sections in order: Hero, About, Work, Projects, Contact
    - No contact form anywhere in ContactSection (per PORT-05)
    - npx tsc --noEmit exits 0
    - next dev shows complete five-section portfolio page at http://localhost:3000
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProjectsSection → external live/GitHub URLs | Outbound links to project deployments and GitHub repos |
| ContactSection → external social profile URLs | Outbound links to GitHub, LinkedIn, X/Twitter profiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | ProjectsSection external links | mitigate | All links target="_blank" rel="noopener noreferrer"; user-confirmed URLs; placeholder format makes unconfirmed URLs obvious |
| T-02-06 | Spoofing | ContactSection social links | mitigate | GitHub hardcoded github.com/yash-278; LinkedIn from user confirmation (Plan 02-01); Twitter hardcoded @yashkadam278; all have noopener noreferrer |
| T-02-07 | Information Disclosure | WorkSection role content | accept | Only public professional info (company names, titles, dates); no PII; no salaries; no internal details |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 02-03; all packages are Phase 1 verified |
</threat_model>

<verification>
After all tasks complete:
- Run: cd /Users/yash/Personal/Portfolio && npm run build — must exit 0, / route must show ○ (static)
- Run next dev — visit http://localhost:3000 — all five sections visible without page reload: Hero, About, Work, Projects, Contact
- Confirm "View my work →" in hero anchor scrolls to the Projects section
- Confirm project card "Live ↗" and "GitHub ↗" links open in new tab
- Confirm contact section shows exactly three social links (no form)
- Confirm section order matches: hero, about, work, projects, contact (matches ROADMAP success criterion 1)
</verification>

<success_criteria>
- PORT-03: Work section with KingsleyGate + previous role(s); title, company (indigo), dates, one impact sentence per role; no bullet points (per D-07)
- PORT-04: Nekomori and Brew Index cards with verbatim descriptions (per D-11), tech stack tags, live link, GitHub link — no older demo projects (per D-09)
- PORT-05: Contact section with GitHub/LinkedIn/Twitter links only — no contact form; LinkedIn URL confirmed with user (resolves WR-04 in ContactSection as well)
- All five sections assembled in app/page.tsx; page is statically generated (no fetch/async)
- next build exits 0; / route is ○ (static) in build output
</success_criteria>

<output>
Create .planning/phases/02-portfolio-page/02-03-SUMMARY.md when done
</output>
