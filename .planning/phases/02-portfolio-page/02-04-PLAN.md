---
phase: 02-portfolio-page
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - app/page.tsx
  - public/og.png
autonomous: false
requirements: [SEO-01]

must_haves:
  truths:
    - "app/page.tsx exports a metadata constant with title, description, openGraph (url, siteName, images array with 1200x630), and twitter (card: summary_large_image)"
    - "OG image URL uses process.env.NEXT_PUBLIC_SITE_URL as base — resolves to https://yashkadam.com/og.png"
    - "public/og.png exists at 1200x630px with dark background and visible 'Yash Kadam' and 'Technical Lead' text"
    - "next build exits 0; / route is marked ○ (static) — no dynamic data fetching warning"
    - "Sharing https://yashkadam.com on a link preview tool (e.g. opengraph.xyz) renders a correctly sized card with title and description"
    - "The page title in browser tab reads 'Yash Kadam — Technical Lead'"
  artifacts:
    - path: "app/page.tsx"
      provides: "Complete page with full Metadata export including OG and twitter fields"
      contains: "openGraph"
    - path: "public/og.png"
      provides: "1200x630 static OG image"
  key_links:
    - from: "app/page.tsx metadata.openGraph.images"
      to: "public/og.png"
      via: "NEXT_PUBLIC_SITE_URL environment variable"
      pattern: "NEXT_PUBLIC_SITE_URL.*og\\.png"
    - from: "app/page.tsx metadata"
      to: "app/layout.tsx metadata"
      via: "Next.js metadata merge — page metadata overrides layout metadata for the / route"
      pattern: "export const metadata"
---

<objective>
Add the full SEO metadata export (OG + Twitter Card) to app/page.tsx and create the static 1200×630 OG image at public/og.png. Then run next build to verify the / route is statically generated and confirm the entire Phase 2 success criteria pass.

Purpose: Delivers SEO-01 (metadata with absolute OG URLs on every page). Completes the Phase 2 goal: the portfolio is statically generated, SEO-tagged, and ready for real traffic.

Output: Updated app/page.tsx with full Metadata export, public/og.png (1200×630 dark static image), and Phase 2 build verification.
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-page/02-CONTEXT.md
@.planning/phases/02-portfolio-page/02-RESEARCH.md
@.planning/phases/02-portfolio-page/02-UI-SPEC.md
@.planning/phases/02-portfolio-page/02-01-SUMMARY.md
@.planning/phases/02-portfolio-page/02-02-SUMMARY.md
@.planning/phases/02-portfolio-page/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add full metadata export to app/page.tsx</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx — read the current file; it has a minimal metadata export (title + description only) from Plan 02-02; the full OG and twitter fields are being added here
    - app/layout.tsx — read the metadata export in the root layout; page-level metadata overrides layout-level metadata for the / route — do NOT duplicate fields that should be page-specific into layout.tsx
    - .planning/phases/02-portfolio-page/02-RESEARCH.md §"Pattern 3: SEO Metadata Export" — exact metadata object shape including the NEXT_PUBLIC_SITE_URL pattern for OG image URL
    - .planning/phases/02-portfolio-page/02-UI-SPEC.md §"SEO Contract" — exact field values for title, description, OG, and twitter
    - .env.local — confirm NEXT_PUBLIC_SITE_URL=https://yashkadam.com is set; this must be set or the OG URL resolves to undefined/og.png
  </read_first>
  <action>
    In app/page.tsx, update the existing metadata export to add the full openGraph and twitter fields. The complete metadata export must be:

    - title: 'Yash Kadam — Technical Lead'
    - description: 'Technical Lead and fullstack developer. I write code, lead teams, and ship things.'
    - openGraph.title: 'Yash Kadam — Technical Lead'
    - openGraph.description: 'Technical Lead and fullstack developer. I write code, lead teams, and ship things.'
    - openGraph.url: 'https://yashkadam.com'
    - openGraph.siteName: 'Yash Kadam'
    - openGraph.images: array with one object { url: `${process.env.NEXT_PUBLIC_SITE_URL}/og.png`, width: 1200, height: 630 }
    - openGraph.type: 'website'
    - twitter.card: 'summary_large_image'
    - twitter.title: 'Yash Kadam — Technical Lead'
    - twitter.description: 'Technical Lead and fullstack developer. I write code, lead teams, and ship things.'
    - twitter.images: array with `${process.env.NEXT_PUBLIC_SITE_URL}/og.png`

    Import type { Metadata } from 'next' if not already imported. The Home() function body must NOT change — keep the five section imports and the fragment with all five sections exactly as built in Plan 02-03. This task adds metadata only. Do NOT add async, fetch(), generateStaticParams, or any dynamic behavior.

    After updating, verify .env.local contains NEXT_PUBLIC_SITE_URL=https://yashkadam.com. If missing, add it. This is required for the OG URL to resolve correctly at build time.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && grep -c "openGraph" app/page.tsx && grep -c "summary_large_image" app/page.tsx && grep -c "NEXT_PUBLIC_SITE_URL" app/page.tsx && grep -c "NEXT_PUBLIC_SITE_URL" .env.local && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - app/page.tsx metadata export has openGraph and twitter fields with all values from the SEO Contract
    - OG image URL uses process.env.NEXT_PUBLIC_SITE_URL template literal (not hardcoded string)
    - .env.local has NEXT_PUBLIC_SITE_URL=https://yashkadam.com
    - npx tsc --noEmit exits 0
    - Home() function body is unchanged from Plan 02-03 (all five sections still present)
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Create static OG image public/og.png</name>
  <what-built>Nothing automated yet — this task creates the 1200×630 static OG image asset.</what-built>
  <how-to-verify>
    The executor should generate public/og.png programmatically using a Node.js canvas script. If canvas generation is not available, ask the user to provide a 1200×630px PNG file.

    Requirements for the OG image:
    - Dimensions: exactly 1200×630px
    - Background: dark (#0a0a0a or similar near-black)
    - Text: "Yash Kadam" in large white or near-white text (at least 64px)
    - Text: "Technical Lead" in muted text below the name (at least 40px)
    - Optional: site URL "yashkadam.com" in small text at bottom
    - Format: PNG
    - File path: public/og.png

    Programmatic approach (recommended — executor runs this as a Node script):
    Use the `canvas` npm package (check if installed; if not, a simpler approach is to use the browser's Canvas API via a script). Alternatively, use a PNG creation approach with raw pixel data if canvas is unavailable. The image does not need to be beautiful — it just needs to pass the 1200×630 OG image standard so link previews render correctly.

    If the user prefers to supply a custom design, ask them to drop it at public/og.png before continuing.
  </how-to-verify>
  <resume-signal>Type "og ready" once public/og.png exists at 1200×630px. The executor will verify dimensions before proceeding.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify OG image dimensions + run final next build</name>
  <files>public/og.png</files>
  <read_first>
    - public/og.png — verify the file exists (check with ls -la public/og.png or file public/og.png)
    - app/page.tsx — read the final state to confirm all five section imports are present and metadata export is complete
    - .planning/ROADMAP.md §Phase 2 Success Criteria — all five criteria must be met before marking this plan done
  </read_first>
  <action>
    Verify public/og.png file dimensions are 1200×630px. Use a Node.js one-liner to check: node -e "const fs = require('fs'); const buf = fs.readFileSync('public/og.png'); const w = buf.readUInt32BE(16); const h = buf.readUInt32BE(20); console.log(w + 'x' + h);" — this reads PNG IHDR dimensions directly from the binary. Expected output: 1200x630. If dimensions are wrong, the OG preview will be cropped or letterboxed by social platforms — fix before proceeding.

    Run the full production build: cd /Users/yash/Personal/Portfolio && npm run build. Check that:
    1. Build exits 0 with no TypeScript errors
    2. The / route is listed as ○ (static) in the build output Routes table — not ƒ (dynamic) or λ (server)
    3. No "Missing NEXT_PUBLIC_SITE_URL" or "undefined/og.png" warnings appear in build output

    If the / route shows as ƒ (dynamic), trace back through all five section components and app/page.tsx for any async function, fetch(), or runtime data access that forces dynamic rendering. Remove it and rebuild.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && test -f public/og.png && node -e "const fs=require('fs');const b=fs.readFileSync('public/og.png');const w=b.readUInt32BE(16);const h=b.readUInt32BE(20);console.log(w+'x'+h);process.exit(w===1200&&h===630?0:1)" && npm run build 2>&1 | grep -E "○|Route|error|Error" | head -20</automated>
  </verify>
  <done>
    - public/og.png exists and dimensions are exactly 1200×630 (verified via PNG IHDR)
    - npm run build exits 0 with zero TypeScript errors
    - / route shown as ○ (static) in build output
    - No "undefined/og.png" or missing env var warnings in build output
    - Phase 2 is complete: five sections rendered, SEO metadata with absolute OG URLs, statically generated
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 2 human verification — five-section portfolio review</name>
  <what-built>
    Complete single-page portfolio with Hero, About, Work History, Projects, and Contact sections. SEO metadata with OG image. Statically generated at build time.
  </what-built>
  <how-to-verify>
    Run: cd /Users/yash/Personal/Portfolio && npm run dev — then visit http://localhost:3000

    Verify each Phase 2 success criterion from ROADMAP.md:

    1. All five sections visible — Can you see Hero, About, Work, Projects, and Contact without a page reload? Scroll through the full page.

    2. Project cards — Do Nekomori and Brew Index cards each show: project name, 1-line description, tech stack tags, "Live ↗" link, "GitHub ↗" link? Do both links open correctly in a new tab?

    3. OG image — Visit https://opengraph.xyz/ (or similar) and enter http://localhost:3000. Does the preview show a card with "Yash Kadam — Technical Lead" title and description? (Note: local testing may require ngrok or deployment for full OG validation — check at minimum that the meta tags are present in page source: View Source → search for "og:image")

    4. No prohibited patterns — Confirm there is NO typewriter animation on the hero name/title, NO animated skill progress bars anywhere, NO older demo projects (Twitter clone, Crown Clothing, etc.)

    5. Static generation — Open the build output (from Task 3 verification) and confirm / route is ○ not ƒ.

    Also verify these quality items:
    - Hero photo visible (not a broken image)
    - Navbar shows Home with indigo underline on the / route
    - "View my work →" in hero scrolls to the Projects section
    - All text is in Geist font (not Inter)
    - Dark background with indigo-400 accent color
    - Contact section has three social icons (GitHub, LinkedIn, X/Twitter) — no form

    Report any issues found.
  </how-to-verify>
  <resume-signal>Type "phase 2 approved" if everything looks correct, or describe any issues found so they can be fixed before marking Phase 2 complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/page.tsx metadata → OG image URL | NEXT_PUBLIC_SITE_URL env var is embedded at build time; missing var produces broken URL |
| public/og.png → social platforms | Static asset served via CDN; no user input; read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | OG image URL with missing env var | mitigate | Task 1 verifies .env.local contains NEXT_PUBLIC_SITE_URL; Task 3 build check catches undefined/og.png |
| T-02-09 | Spoofing | openGraph.url hardcoded as https://yashkadam.com | accept | Canonical URL is the owner's own domain; intentional hardcoding; low risk |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 02-04; all packages Phase 1 verified |
</threat_model>

<verification>
Phase 2 complete when all of the following pass:
1. npm run build exits 0 with / route ○ (static) — ROADMAP success criterion 5
2. public/og.png exists at 1200×630px
3. app/page.tsx exports metadata with openGraph images array pointing to NEXT_PUBLIC_SITE_URL/og.png
4. Five sections visible at http://localhost:3000 — ROADMAP success criterion 1
5. Project cards for Nekomori and Brew Index with live + GitHub links — ROADMAP success criterion 2
6. OG meta tags present in page source — ROADMAP success criterion 3
7. No typewriter animation, no skill bars, no older demo projects — ROADMAP success criterion 4
8. Human approval received in Task 4 checkpoint
</verification>

<success_criteria>
- SEO-01: metadata export in app/page.tsx has title, description, openGraph (url, siteName, images with absolute HTTPS URL), and twitter (card: summary_large_image) — absolute OG image URL using NEXT_PUBLIC_SITE_URL
- public/og.png exists at 1200×630px
- next build exits 0; / route is ○ (static)
- Phase 2 ROADMAP success criteria 1-5 all pass (human-verified in Task 4)
</success_criteria>

<output>
Create .planning/phases/02-portfolio-page/02-04-SUMMARY.md when done
</output>
