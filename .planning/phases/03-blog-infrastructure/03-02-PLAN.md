---
phase: 03-blog-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/blog/page.tsx
  - app/blog/[slug]/page.tsx
  - content/blog/building-nekomori.mdx
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02

must_haves:
  truths:
    - "Visiting /blog in the browser shows a list of posts with title, date (formatted as 'May 24, 2026'), description, and reading time — sorted newest first"
    - "The blog index shows only published posts — test-post.mdx does not appear"
    - "Clicking a post title on the blog index navigates to /blog/[slug] which renders the MDX content"
    - "The post page shows a '← All posts' link above the post title that returns to /blog"
    - "Code blocks in the post render with github-dark syntax highlighting (colored tokens, not plain text)"
    - "Post body text uses readable prose typography via prose dark:prose-invert — headings are distinct, paragraphs have proper line height"
    - "The building-nekomori.mdx seed post has all required frontmatter fields and published: true so it appears on the index"
    - "next build shows /blog and /blog/[slug] as statically generated (SSG) routes"
  artifacts:
    - path: "app/blog/page.tsx"
      provides: "Blog index Server Component — calls getAllPosts(), renders title/date/description/readingTime list"
      exports: ["default", "metadata"]
    - path: "app/blog/[slug]/page.tsx"
      provides: "Dynamic post Server Component — async params, generateStaticParams, dynamic MDX import, prose wrapper"
      exports: ["default", "generateStaticParams", "dynamicParams"]
    - path: "content/blog/building-nekomori.mdx"
      provides: "Seed post — Nekomori technical write-up with full frontmatter and a code block"
      contains: "published: true"
  key_links:
    - from: "app/blog/page.tsx"
      to: "lib/posts.ts"
      via: "import { getAllPosts } from '@/lib/posts'"
      pattern: "getAllPosts"
    - from: "app/blog/[slug]/page.tsx"
      to: "lib/posts.ts"
      via: "import { getAllPosts } from '@/lib/posts'"
      pattern: "getAllPosts"
    - from: "app/blog/[slug]/page.tsx"
      to: "content/blog/*.mdx"
      via: "await import(`@/content/blog/${slug}.mdx`)"
      pattern: "import.*content/blog.*\\.mdx"
    - from: "app/blog/[slug]/page.tsx"
      to: "app/blog/layout.tsx"
      via: "Next.js App Router layout wrapping"
      pattern: "max-w-3xl"
---

<objective>
Replace the two blog stub pages with real implementations that deliver the complete working blog: an index page at /blog listing published posts with metadata, and a post detail page at /blog/[slug] that renders MDX content with prose typography and syntax highlighting. Also write the Nekomori seed post.

Purpose: This plan completes all Phase 3 success criteria. After Plan 01 wired the infrastructure, this plan surfaces it to users — the blog is fully functional and the phase is done.

Output: app/blog/page.tsx (real list page), app/blog/[slug]/page.tsx (dynamic MDX post page), content/blog/building-nekomori.mdx (seed post with real technical content).
</objective>

<execution_context>
@/Users/yash/Personal/Portfolio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/Personal/Portfolio/.planning/phases/03-blog-infrastructure/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** developer visiting yashkadam.com, **I want to** read Yash's technical blog posts with proper typography and syntax highlighting, **so that** I can learn from his work and understand his engineering depth.

<context>
@/Users/yash/Personal/Portfolio/.planning/ROADMAP.md
@/Users/yash/Personal/Portfolio/.planning/phases/03-blog-infrastructure/03-CONTEXT.md
@/Users/yash/Personal/Portfolio/.planning/phases/03-blog-infrastructure/03-RESEARCH.md
@/Users/yash/Personal/Portfolio/.planning/phases/03-blog-infrastructure/03-PATTERNS.md
@/Users/yash/Personal/Portfolio/.planning/phases/03-blog-infrastructure/03-01-SUMMARY.md
@/Users/yash/Personal/Portfolio/app/page.tsx
@/Users/yash/Personal/Portfolio/app/blog/page.tsx
@/Users/yash/Personal/Portfolio/app/blog/[slug]/page.tsx
@/Users/yash/Personal/Portfolio/components/ProjectsSection.tsx
@/Users/yash/Personal/Portfolio/lib/posts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace app/blog/page.tsx with real blog index</name>
  <files>app/blog/page.tsx</files>
  <read_first>
    - app/blog/page.tsx — read current stub before replacing
    - app/page.tsx — follow the Metadata export pattern (lines 8-34) and Server Component structure
    - components/ProjectsSection.tsx — follow the list-with-items rendering pattern (lines 35-69) and typography token conventions
    - lib/posts.ts — read PostMeta interface to understand the data shape
    - .planning/phases/03-blog-infrastructure/03-CONTEXT.md — D-04 (minimal list layout), D-05 (newest first), D-06 (back link applies to post page), D-07 (layout provides container)
    - .planning/phases/03-blog-infrastructure/03-PATTERNS.md — app/blog/page.tsx section for analog patterns
  </read_first>
  <behavior>
    - Page exports `metadata` with `title: 'Blog — Yash Kadam'` and a relevant description
    - Page calls `getAllPosts()` from `@/lib/posts` and renders one list item per post
    - Each post item shows: title as a Link to `/blog/${post.slug}` (styled with text-text hover:text-accent), date formatted as "May 24, 2026" using Intl.DateTimeFormat, description as text-text-muted, reading time (post.readingTime) in font-mono text-xs text-text-muted
    - Posts are already sorted by getAllPosts() — no additional sort in the page
    - Date is formatted inline with a local function using `new Intl.DateTimeFormat('en-US', { year: 'numeric', month: 'long', day: 'numeric' }).format(new Date(isoDate))` — no extra date library
    - Page has no 'use client' directive — Server Component
    - The list is stacked vertically with dividers or space-y-* spacing, not a card grid (per D-04)
    - Tag pills rendered using the same pattern as ProjectsSection: `rounded-md bg-surface px-2.5 py-1 font-mono text-xs text-text-muted`
    - If getAllPosts() returns an empty array, render a fallback message like "No posts yet." in text-text-muted
  </behavior>
  <action>
    Replace the entire contents of app/blog/page.tsx. No 'use client'. Import: `type Metadata` from `'next'`, `Link` from `'next/link'`, `getAllPosts` from `'@/lib/posts'`.

    Export metadata: `title: 'Blog — Yash Kadam'`, `description: 'Technical writing on fullstack development, TypeScript, and building products at scale.'`

    Define a local helper function `formatDate(isoDate: string): string` using `Intl.DateTimeFormat` with `en-US` locale, `year: 'numeric'`, `month: 'long'`, `day: 'numeric'`. Returns formatted string like "May 24, 2026".

    Default export: async Server Component. Call `const posts = getAllPosts()` at the top (no await needed — getAllPosts is synchronous). Render a page heading "Writing" or "Blog" in the project's heading style (text-text font-semibold text-3xl or text-4xl), then the post list.

    Post list: `<ul className="mt-10 divide-y divide-border">`. Each item: `<li key={post.slug} className="py-8">`. Inside: title as `<Link href={"/blog/" + post.slug}>` with `className="text-lg font-semibold text-text transition-colors duration-150 hover:text-accent"`. Below title: `<p className="mt-1 text-xs font-mono text-text-muted">{formatDate(post.date)} · {post.readingTime}</p>`. Below that: `<p className="mt-2 text-sm leading-relaxed text-text-muted">{post.description}</p>`. If post.tags.length > 0, render a `<div className="mt-3 flex flex-wrap gap-2">` with tag pills `<span className="rounded-md bg-surface px-2.5 py-1 font-mono text-xs text-text-muted">{tag}</span>`.

    If `posts.length === 0`, render `<p className="mt-10 text-sm text-text-muted">No posts yet.</p>` instead of the list.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && npx tsc --noEmit 2>&1 | grep -c 'blog/page' || echo "0 type errors in blog/page"</automated>
  </verify>
  <done>app/blog/page.tsx exports metadata and a default Server Component. `grep -c 'getAllPosts' app/blog/page.tsx` returns 1. `grep -c 'formatDate' app/blog/page.tsx` returns at least 1. `grep -c 'divide-border' app/blog/page.tsx` returns 1. TypeScript compiles without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace app/blog/[slug]/page.tsx with dynamic MDX post renderer</name>
  <files>app/blog/[slug]/page.tsx</files>
  <read_first>
    - app/blog/[slug]/page.tsx — read current stub (has wrong sync params type — must replace entirely)
    - .planning/phases/03-blog-infrastructure/03-RESEARCH.md — Pattern 3: Dynamic MDX post page (critical: async params, dynamic import with .mdx extension, dynamicParams=false)
    - .planning/phases/03-blog-infrastructure/03-PATTERNS.md — app/blog/[slug]/page.tsx section for exact patterns including back-link and prose wrapper
    - .planning/phases/03-blog-infrastructure/03-CONTEXT.md — D-06 (← All posts link), D-08 (github-dark via prose wrapper)
    - lib/posts.ts — read getAllPosts() and PostMeta to understand available data for metadata generation
  </read_first>
  <behavior>
    - `params` is typed as `Promise<{ slug: string }>` and awaited with `const { slug } = await params` — NOT synchronous destructuring
    - `generateStaticParams()` calls `getAllPosts()` and maps to `{ slug: post.slug }` array
    - `export const dynamicParams = false` is set so unknown slugs return 404 (not runtime error)
    - Dynamic import uses template literal with explicit `.mdx` extension: `await import(\`@/content/blog/${slug}.mdx\`)` — missing extension causes module-not-found at build time
    - If the dynamic import throws (slug not found), `notFound()` from `'next/navigation'` is called
    - The `<article>` element has `className="prose dark:prose-invert max-w-none"` — prose classes are on the article, NOT on the blog layout
    - A `← All posts` link rendered as `<Link href="/blog">` appears ABOVE the `<article>` element (per D-06)
    - The back link uses `className` containing `text-text-muted hover:text-accent font-mono text-xs transition-colors duration-150`
    - Page generates per-post metadata with the post's title and description (requires reading frontmatter from getAllPosts())
    - No 'use client' directive
  </behavior>
  <action>
    Replace the entire contents of app/blog/[slug]/page.tsx. No 'use client'. Imports: `type Metadata` from `'next'`, `{ notFound }` from `'next/navigation'`, `Link` from `'next/link'`, `{ getAllPosts }` from `'@/lib/posts'`.

    Export `generateStaticParams()`: `return getAllPosts().map((post) => ({ slug: post.slug }))`.

    Export `export const dynamicParams = false`.

    Export `generateMetadata` async function: accepts `{ params }: { params: Promise<{ slug: string }> }`, awaits params, finds matching post from `getAllPosts()`, returns `{ title: \`${post.title} — Yash Kadam\`, description: post.description }`. If post not found, return `{ title: 'Post Not Found' }`.

    Default export: `async function BlogPost({ params }: { params: Promise<{ slug: string }> })`. Body: `const { slug } = await params`. Declare `let Post: React.ComponentType` (import React from 'react' for the type, or use `React.ComponentType` with implicit React types from tsconfig). Inside try/catch: `const module = await import(\`@/content/blog/${slug}.mdx\`)` (literal backtick template, .mdx extension required); `Post = module.default`. Catch block: `notFound()`. Return JSX:

    Outer wrapper: `<>`. First child: `<Link href="/blog" className="mb-8 inline-flex items-center gap-1 font-mono text-xs text-text-muted transition-colors duration-150 hover:text-accent">← All posts</Link>`. Second child: `<article className="prose dark:prose-invert max-w-none"><Post /></article>`. Close `<>`.
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && npx tsc --noEmit 2>&1 | grep -c 'blog/\[slug\]' || echo "0 type errors in slug page"</automated>
  </verify>
  <done>`grep -c 'Promise.*slug' "app/blog/[slug]/page.tsx"` returns 1 (async params type). `grep -c 'dynamicParams = false' "app/blog/[slug]/page.tsx"` returns 1. `grep -c 'prose dark:prose-invert' "app/blog/[slug]/page.tsx"` returns 1. `grep -c '.mdx' "app/blog/[slug]/page.tsx"` returns at least 1 (dynamic import extension). TypeScript compiles without errors. `next build` shows /blog/[slug] as SSG in output.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Nekomori seed post</name>
  <files>content/blog/building-nekomori.mdx</files>
  <read_first>
    - content/blog/test-post.mdx — follow the MDX file structure (frontmatter delimiter, heading, prose, fenced code blocks)
    - .planning/phases/03-blog-infrastructure/03-CONTEXT.md — D-09 (Nekomori post, Claude picks technical angle), D-01 (frontmatter schema), D-03 (no cover image field)
    - .planning/phases/03-blog-infrastructure/03-PATTERNS.md — content/blog/building-nekomori.mdx section
    - components/ProjectsSection.tsx — read Nekomori project description to understand what the project does
  </read_first>
  <action>
    Create content/blog/building-nekomori.mdx as a new file. The most interesting technical angle for Nekomori (anime schedule tracker / personal watchlist) is the data sync architecture: how to keep local watchlist state consistent with external anime data (MAL/AniList APIs) without hammering rate limits, and how optimistic UI updates work alongside the background sync.

    Frontmatter (per D-01, D-09):
    - title: "Building Nekomori: Sync Without the Pain"
    - date: "2026-05-20" (a few days before today so it shows as recent)
    - description: "How I designed the data sync layer for Nekomori — keeping local watchlist state consistent with external anime APIs without hammering rate limits."
    - tags: ["nextjs", "typescript", "architecture", "react"]
    - published: true

    Post structure: Write ~600-800 words of genuine technical prose covering: (1) the problem — users track anime from multiple sources, sync state is hard; (2) the architecture decision — local-first state with background sync, not request-on-every-visit; (3) the implementation — SWR for client-side cache, a background job polling the MAL API within rate limits, an optimistic update pattern for watch status changes; (4) a real TypeScript code block (30-50 lines) showing a key part of the sync logic or the watchlist update function; (5) lessons learned. Use h2 subheadings. Include at least one TypeScript fenced code block with realistic identifiers. Write as Yash's genuine voice — direct, technical, no fluff.

    The post must contain a fenced code block using ` ```typescript ` syntax (so rehype-pretty-code syntax highlighting is exercised).
  </action>
  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && grep -c 'published: true' content/blog/building-nekomori.mdx && grep -c '```typescript' content/blog/building-nekomori.mdx</automated>
  </verify>
  <done>content/blog/building-nekomori.mdx exists with all five required frontmatter fields (title, date, description, tags, published: true). Contains at least one ` ```typescript ` fenced code block. Body is at least 400 words of technical prose. `next build` generates /blog/building-nekomori as a static route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug → dynamic import path | Slug from URL becomes import path; constrained to generateStaticParams output |
| MDX content → rendered HTML | Author-controlled MDX (Yash's own content); no user-submitted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | app/blog/[slug]/page.tsx dynamic import | mitigate | `dynamicParams = false` restricts valid slugs to generateStaticParams() output only; unknown slugs hit notFound() rather than attempting arbitrary module resolution |
| T-03-04 | Information Disclosure | app/blog/[slug]/page.tsx import error | mitigate | try/catch around dynamic import calls notFound() — no error details leak to the response |
| T-03-SC | Tampering | npm installs | accept | No new installs in this plan — all packages already present |
</threat_model>

<verification>
After all three tasks complete, these must all pass:

1. `npx tsc --noEmit` exits 0
2. `next build` completes successfully with /blog and /blog/[slug] shown as static routes (○ symbol in build output)
3. `grep -c 'getAllPosts' app/blog/page.tsx` returns 1
4. `grep -c 'Promise.*slug' "app/blog/[slug]/page.tsx"` returns 1 (async params type)
5. `grep -c 'dynamicParams = false' "app/blog/[slug]/page.tsx"` returns 1
6. `grep -c 'prose dark:prose-invert' "app/blog/[slug]/page.tsx"` returns 1
7. `grep -c 'published: true' content/blog/building-nekomori.mdx` returns 1
8. `grep -c '```typescript' content/blog/building-nekomori.mdx` returns at least 1

Phase 3 Success Criteria check:
- SC1: `next dev` — /blog lists building-nekomori with title, date, description, reading time ✓
- SC2: /blog/building-nekomori renders MDX with prose typography and syntax-highlighted code; no visible YAML ✓
- SC3: getAllPosts() returns array with all six required fields for every published .mdx file ✓
</verification>

<success_criteria>
- /blog page renders a real post list from getAllPosts() with per-post: linked title, formatted date, description, reading time, tag pills
- /blog/building-nekomori renders the Nekomori seed post with prose dark:prose-invert typography and github-dark syntax highlighting on code blocks
- The ← All posts back link appears above every post article and returns to /blog
- test-post.mdx does NOT appear on /blog (published: false)
- `next build` exits 0 with /blog and /blog/[slug] as SSG routes (no server-side rendering or client-side data fetching)
- Phase 3 ROADMAP success criteria 1, 2, and 3 are all TRUE
</success_criteria>

<output>
Create `.planning/phases/03-blog-infrastructure/03-02-SUMMARY.md` when done.
</output>
