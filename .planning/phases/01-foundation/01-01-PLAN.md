---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - FOUND-01
must_haves:
  truths:
    - "Branch feat/next16-foundation exists and is checked out (per D-03)"
    - "next dev starts without error on Next.js 16.2.6"
    - "next build exits 0 with zero TypeScript errors"
    - "tailwindcss version in package.json is ^3.4.x (not v4)"
    - "All production and dev dependencies are installed and node_modules is present"
  artifacts:
    - path: "package.json"
      provides: "Fresh Next.js 16.2.6 scaffold dependencies"
      contains: "\"next\": \"16.2.6\""
    - path: "app/layout.tsx"
      provides: "Scaffold root layout (will be replaced in Plan 03)"
  key_links:
    - from: "package.json"
      to: "node_modules/next"
      via: "npm install"
      pattern: "\"next\":\\s*\"16\\.2\\.6\""

user_setup:
  - service: vercel
    why: "FOUND-06 — NEXT_PUBLIC_SITE_URL must be set in Vercel before first build (handled in Plan 04)"
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "Vercel Dashboard -> Project Settings -> Environment Variables (set in Plan 04)"
---

<objective>
Create the git branch (per D-03), verify all [ASSUMED] packages in the legitimacy audit, scaffold a fresh Next.js 16.2.6 project (per D-01), and install all production + dev dependencies with Tailwind pinned to v3.

Purpose: Every subsequent plan builds on top of a clean, correctly versioned scaffold. Doing this first in its own plan isolates the riskiest step (create-next-app + dependency install) and surfaces any version mismatch before any code is written.

Output: Branch feat/next16-foundation, fresh Next.js 16.2.6 app with all deps installed, Tailwind v3 confirmed.
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify [ASSUMED] package legitimacy before install</name>
  <what-built>
    The RESEARCH.md Package Legitimacy Audit lists all packages tagged [ASSUMED] because slopcheck was unavailable at research time. Before any npm install runs, verify the key packages are legitimate by spot-checking 3-4 of the highest-risk entries (non-Vercel, non-Meta packages) against npmjs.com.

    Packages to spot-check (open each URL and confirm: correct source repo, reasonable download count, recent publish date):
    - https://www.npmjs.com/package/rehype-pretty-code — expect: github.com/rehype-pretty/rehype-pretty-code, ~3 yrs old, high downloads
    - https://www.npmjs.com/package/lucide-react — expect: github.com/lucide-icons/lucide, ~4 yrs old, high downloads
    - https://www.npmjs.com/package/tailwind-merge — expect: github.com/dcastil/tailwind-merge, ~3 yrs old, very high downloads
    - https://www.npmjs.com/package/clsx — expect: github.com/lukeed/clsx, ~7 yrs old, very high downloads

    The well-known packages (next, react, framer-motion, @next/mdx, @mdx-js/*, shiki, prettier, prettier-plugin-tailwindcss) are pre-approved — no manual check needed for those.
  </what-built>
  <how-to-verify>
    1. Open each URL above in a browser.
    2. For each: confirm the Source repository matches the expected GitHub org/repo.
    3. Confirm Weekly Downloads is not suspiciously low (rehype-pretty-code: >100k/wk, lucide-react: >1M/wk, tailwind-merge: >5M/wk, clsx: >10M/wk).
    4. Confirm Published date is recent (within last 12 months for active packages).
    5. No package name typos or unexpected publishers.
  </how-to-verify>
  <resume-signal>Type "packages verified" to proceed with scaffold, or describe any suspicious findings.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create branch, scaffold Next.js 16.2.6, install all dependencies</name>
  <files>package.json, package-lock.json, app/layout.tsx, app/page.tsx, app/globals.css, next.config.mjs, tailwind.config.ts, tsconfig.json, postcss.config.mjs, .env.local.example</files>

  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section, Environment Availability section, Pitfall 5 on Tailwind v4 detection)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 fresh scaffold, D-03 branch name, D-08 font deps, D-09 drop v2 fonts)
    - .planning/phases/01-foundation/01-SKELETON.md (Framework & Bundler and Styling decisions)
  </read_first>

  <action>
    Step 1 — Create and checkout the feature branch (per D-03):
    Run: git checkout -b feat/next16-foundation

    Step 2 — Scaffold the app in a temp directory, then move files:
    The current repo root already has the old vanilla HTML/CSS/JS portfolio files (index.html, src/, dist/, etc.). The new Next.js app must be scaffolded cleanly.

    Run create-next-app in a subdirectory named `next-app`, then move its contents to repo root:
      npx create-next-app@16.2.6 next-app --typescript --tailwind --eslint --app --src-dir=no --import-alias="@/*" --no-git
    
    After scaffold completes, move all generated files from next-app/ to repo root (overwriting scaffold defaults where needed). The old vanilla files (index.html, src/, dist/, tailwind.config.js, README.md) remain in the repo but are separate from the Next.js app — they will be cleaned up in Phase 2.

    Step 3 — Verify Tailwind version:
    Open the generated package.json. Confirm tailwindcss entry is `^3.x.x` (e.g., `^3.4.0`). If it shows `^4.x.x`, remove it and reinstall with: npm install tailwindcss@"^3.4" --save-dev. Tailwind v4 has no tailwind.config.ts — its absence is the warning sign (per RESEARCH.md Pitfall 5).

    Step 4 — Install production dependencies (per RESEARCH.md Standard Stack):
      npm install framer-motion lucide-react clsx tailwind-merge @next/mdx @mdx-js/loader @mdx-js/react @types/mdx rehype-pretty-code shiki

    Step 5 — Install dev dependencies:
      npm install -D prettier prettier-plugin-tailwindcss

    Step 6 — Create .env.local.example at repo root:
    File content: NEXT_PUBLIC_SITE_URL=https://yashkadam.com
    This documents the required env var per FOUND-06 without committing a real value.

    Step 7 — Create content/blog/ directory (per SKELETON.md directory layout):
    Run: mkdir -p content/blog
    This directory is empty in Phase 1 — it is the future home of MDX post files.
  </action>

  <verify>
    <automated>
      cd /Users/yash/Personal/Portfolio && node -e "const p = require('./package.json'); const v = p.dependencies?.next || p.devDependencies?.next; console.log('next:', v); const tw = p.dependencies?.tailwindcss || p.devDependencies?.tailwindcss; console.log('tailwind:', tw); process.exit(v && tw ? 0 : 1)"
    </automated>
  </verify>

  <acceptance_criteria>
    - package.json contains `"next": "16.2.6"` (exact version, not range)
    - package.json contains `"tailwindcss": "^3.4` (v3 series, not v4)
    - package.json contains `"framer-motion"`, `"lucide-react"`, `"clsx"`, `"tailwind-merge"`, `"@next/mdx"`, `"@mdx-js/loader"`, `"@mdx-js/react"`, `"@types/mdx"`, `"rehype-pretty-code"`, `"shiki"` in dependencies
    - package.json contains `"prettier"` and `"prettier-plugin-tailwindcss"` in devDependencies
    - node_modules/ directory exists and is non-empty
    - .env.local.example exists at repo root and contains the line `NEXT_PUBLIC_SITE_URL=https://yashkadam.com`
    - content/blog/ directory exists
    - `next dev` starts without errors (run manually to confirm, or verify via: npx next info showing version 16.2.6)
    - git branch shows `feat/next16-foundation` as current branch
  </acceptance_criteria>

  <done>Branch feat/next16-foundation exists, Next.js 16.2.6 scaffold is in place at repo root, all production and dev dependencies installed, Tailwind confirmed at v3, content/blog/ directory created, .env.local.example documents NEXT_PUBLIC_SITE_URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party package code enters the local filesystem during install |
| GitHub remote → local branch | git checkout/push touches remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install (all [ASSUMED] packages) | mitigate | Human checkpoint before install; spot-check rehype-pretty-code, lucide-react, tailwind-merge, clsx on npmjs.com |
| T-01-02 | Information Disclosure | .env.local.example | accept | File only contains the env var name and a placeholder public URL — no secrets; safe to commit |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable at research time; human checkpoint (Task 1) gates [ASSUMED] packages before install |

</threat_model>

<verification>
Run after Task 2 completes:

1. Confirm branch: `git branch --show-current` must output `feat/next16-foundation`
2. Confirm Next.js version: `node -e "console.log(require('./node_modules/next/package.json').version)"` must output `16.2.6`
3. Confirm Tailwind v3: `node -e "console.log(require('./node_modules/tailwindcss/package.json').version)"` must start with `3.`
4. Confirm key deps installed: check node_modules contains `framer-motion`, `lucide-react`, `@next/mdx`, `rehype-pretty-code`, `shiki`
5. Confirm .env.local.example exists: `cat .env.local.example` outputs `NEXT_PUBLIC_SITE_URL=https://yashkadam.com`
6. Confirm content/blog/ exists: `ls content/blog/`
</verification>

<success_criteria>
- Branch feat/next16-foundation is active
- next package is 16.2.6 in node_modules
- tailwindcss is v3.x in node_modules
- All packages from the Standard Stack are installed
- .env.local.example documents NEXT_PUBLIC_SITE_URL
- content/blog/ directory created
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
