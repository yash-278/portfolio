---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - content/blog/test-post.mdx
autonomous: false
requirements:
  - FOUND-04
  - FOUND-06
must_haves:
  truths:
    - "A .mdx file at content/blog/test-post.mdx renders as HTML — not raw markdown text"
    - "NEXT_PUBLIC_SITE_URL=https://yashkadam.com is set in Vercel project env vars"
    - "All 5 Phase 1 success criteria from ROADMAP.md are verifiably true"
    - "Branch feat/next16-foundation is ready for PR merge to main"
  artifacts:
    - path: "content/blog/test-post.mdx"
      provides: "Smoke-test MDX file that proves the pipeline works"
      contains: "# Test Post"
  key_links:
    - from: "content/blog/test-post.mdx"
      to: "mdx-components.tsx"
      via: "@next/mdx compile-time transform"
      pattern: "useMDXComponents"
---

<objective>
End-to-end smoke test: confirm the MDX pipeline renders an .mdx file as HTML (not raw markdown), set NEXT_PUBLIC_SITE_URL in Vercel env vars via Vercel CLI, and verify all 5 Phase 1 success criteria from ROADMAP.md before preparing the PR.

Purpose: This plan closes the loop — it proves the infrastructure actually works end-to-end. A first-time visitor can land on a dark-mode site with a working Navbar and Footer, and a blog route will eventually resolve to rendered MDX content (not broken markup).

Output: Verified MDX pipeline, NEXT_PUBLIC_SITE_URL in Vercel env vars, all 5 success criteria confirmed, branch ready for PR.
</objective>

<execution_context>
@/Users/yash/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create MDX smoke-test file and verify pipeline renders HTML</name>
  <files>content/blog/test-post.mdx, app/blog/[slug]/page.tsx</files>

  <read_first>
    - content/blog/ (verify directory exists — created in Plan 01)
    - mdx-components.tsx (verify useMDXComponents is in place)
    - next.config.mjs (verify MDX pipeline config is present — mdxRs: false, rehypePrettyCode)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 4: mdx-components.tsx location, Architecture Patterns section — @next/mdx App Router pattern)
  </read_first>

  <action>
    Step 1 — Create content/blog/test-post.mdx:
    Write a minimal MDX file to smoke-test the pipeline. The file content must include:
    - A YAML frontmatter block (between --- delimiters) with: title, date, description
    - An H1 heading: `# Test Post`
    - A short paragraph
    - A fenced TypeScript code block (3 or more lines) — this tests rehype-pretty-code syntax highlighting

    Step 2 — Create app/blog/[slug]/page.tsx:
    Create the directory app/blog/[slug]/ and the page.tsx file inside it. This is a minimal smoke-test route — Phase 3 will replace it with the real blog post page.

    The file must:
    - Be a Server Component (no `'use client'`)
    - Import the MDX file dynamically using Next.js dynamic import or static import — use the pattern that @next/mdx App Router supports: `import Post from '@/content/blog/[slug that matches param].mdx'` — for the smoke test, hardcode the import of `test-post.mdx`
    - Render the imported MDX component inside a `<article>` element
    - Accept `params: { slug: string }` as a prop and use `generateStaticParams` returning `[{ slug: 'test-post' }]` so the route is statically generated

    The MDX import pattern for @next/mdx in App Router:
    - Static import: `import TestPost from '@/content/blog/test-post.mdx'` — works because @next/mdx treats .mdx as a module
    - For a real dynamic blog, Phase 3 will switch to fs-based discovery + dynamic import

    Step 3 — Verify MDX renders:
    Run `npx next build` — must exit 0. The build output should show `/blog/test-post` as a statically generated route.
    Run `npx next start` (or `npx next dev`) and visit `http://localhost:3000/blog/test-post` — the page must show rendered HTML (an H1 heading and syntax-highlighted code block), NOT raw markdown text (no visible `#` characters, no triple backticks).
  </action>

  <verify>
    <automated>cd /Users/yash/Personal/Portfolio && npx next build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - content/blog/test-post.mdx exists and contains `# Test Post` heading
    - content/blog/test-post.mdx contains YAML frontmatter (--- block with title, date, description fields)
    - content/blog/test-post.mdx contains a fenced code block with a language tag (e.g., ```typescript)
    - app/blog/[slug]/page.tsx exists and imports the .mdx file via @next/mdx static import
    - `npx next build` exits 0 with `/blog/test-post` listed as a static route in build output
    - Visiting http://localhost:3000/blog/test-post shows an H1 element (not raw `# Test Post` text)
    - Raw markdown text (triple backticks, # characters as text) is NOT visible in the browser output
  </acceptance_criteria>

  <done>MDX pipeline confirmed end-to-end: test-post.mdx compiles to rendered HTML with syntax-highlighted code blocks visible in browser. next build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Set NEXT_PUBLIC_SITE_URL in Vercel and verify all 5 Phase 1 success criteria</name>
  <what-built>
    Task 1 confirmed the MDX pipeline. Claude will now attempt to set NEXT_PUBLIC_SITE_URL via the Vercel CLI:

    Run: `vercel env add NEXT_PUBLIC_SITE_URL production`
    When prompted, enter the value: https://yashkadam.com
    Repeat for preview environment if needed.

    After the env var is set, the following 5 Phase 1 success criteria from ROADMAP.md must be verified:

    1. `npx next build` exits 0 with zero TypeScript errors on Next.js 16.2.6
    2. Loading any page in a browser with dark-mode preference shows NO white flash before dark theme applies
    3. mdx-components.tsx exists at project root AND visiting /blog/test-post renders HTML (not raw markdown)
    4. No `motion.*` import exists anywhere — `grep -r "import { motion" app/ components/` returns nothing
    5. Navbar and Footer are visible on every page route; NEXT_PUBLIC_SITE_URL is set in Vercel
  </what-built>
  <how-to-verify>
    Before typing "approved", confirm each item:

    1. Build: Run `npx next build` in the terminal — confirm output shows "Compiled successfully" or equivalent, zero TypeScript errors, exit code 0.

    2. FOUC check: Open http://localhost:3000 in a browser that has dark mode enabled in OS settings. Watch the page load carefully — there must be NO white background flash before the dark theme appears. The page should be dark from the very first frame.

    3. MDX pipeline: Confirm mdx-components.tsx exists at project root (not inside app/). Visit http://localhost:3000/blog/test-post — you must see a rendered H1 heading "Test Post" and syntax-highlighted code block. Do NOT see raw # characters or triple backticks.

    4. No motion.* leak: Run in terminal: `grep -r "import { motion" app/ components/` — must return ZERO results.

    5. Layout shell + env var:
       - Visit http://localhost:3000 — Navbar with "Yash Kadam", "Home", "Blog" links visible at top
       - Visit http://localhost:3000/blog/test-post — same Navbar and Footer visible
       - Confirm in Vercel Dashboard (https://vercel.com/dashboard → your project → Settings → Environment Variables) OR via CLI (`vercel env ls`) that NEXT_PUBLIC_SITE_URL=https://yashkadam.com is set for production.

    If the Vercel CLI auth fails (not logged in), log in with `vercel login` and retry, or set the env var manually in the Vercel dashboard.
  </how-to-verify>
  <resume-signal>Type "all 5 criteria verified" when all checks pass, or describe which criterion failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/blog/*.mdx → @next/mdx compiler | MDX files are parsed and executed as JSX at build time |
| Vercel CLI → Vercel API | env var value sent to Vercel infrastructure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | MDX files as build-time JSX | accept | content/blog/*.mdx files are authored by the developer (Yash); no user-submitted MDX content in this project; build-time execution is trusted |
| T-04-02 | Information Disclosure | NEXT_PUBLIC_SITE_URL env var | accept | Value is a public URL (https://yashkadam.com) — no sensitive data; NEXT_PUBLIC_ prefix is intentionally public |
| T-04-03 | Information Disclosure | Vercel CLI authentication | accept | CLI uses OAuth token stored in ~/.local/share/com.vercel.cli — not committed to repo |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new installs in this plan |

</threat_model>

<verification>
Final Phase 1 verification checklist (all 5 ROADMAP success criteria):

1. `npx next build` exits 0 — TypeScript strict mode passes, no errors
2. Browser dark mode preference → no white flash on any route
3. `ls mdx-components.tsx` at project root exists + `http://localhost:3000/blog/test-post` renders HTML
4. `grep -r "import { motion" app/ components/` returns zero results
5. Navbar + Footer visible on `/` and `/blog/test-post`; `vercel env ls | grep NEXT_PUBLIC_SITE_URL` shows the value

Run `git log --oneline -5` to confirm work is committed on `feat/next16-foundation` branch.
</verification>

<success_criteria>
- content/blog/test-post.mdx renders as HTML at /blog/test-post (FOUND-04 verified end-to-end)
- NEXT_PUBLIC_SITE_URL=https://yashkadam.com set in Vercel project env vars (FOUND-06)
- All 5 Phase 1 success criteria from ROADMAP.md are verified
- feat/next16-foundation branch is clean and ready for PR to main (per D-03)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
